# 第 2 章 消息与上下文管理

> 上一章：[第 1 章 模型接入层](01-模型接入层.md) ｜ 下一章：[第 3 章 Agent 循环](03-Agent循环.md)

---

## 一、概念：上下文是稀缺资源

LLM 的上下文窗口是有限的（几万到几十万 token）。但一个 agent 会话会无限变长：用户说的话、模型的回答、每次工具调用的输入输出……很快就塞满窗口。

**消息与上下文管理**要解决的问题就是：

1. **喂给模型什么**：一次 LLM 请求里，到底放哪些消息？
2. **放不下了怎么办**：裁剪、压缩、还是丢弃？
3. **原始历史怎么保留**：压缩是有损的，被压缩掉的内容还能找回吗？

这一层的本质，是在「**有限的上下文窗口**」和「**无限增长的会话**」之间做取舍。

---

## 二、原理：两层消息模型 + 转换管道

最优雅的设计（pi 的贡献）是把消息分成**两层**：

```
内部消息（AgentMessage）         LLM 消息（LlmMessage）
  丰富、可扩展、面向应用           精简、固定、面向模型
       │                                ▲
       │     转换管道（每次调模型前）      │
       └────────────────────────────────┘
```

- **`AgentMessage`**（内部）：可以带任意应用级信息——UI 专用消息、系统事件、自定义类型、时间戳、父子关系等。
- **`LlmMessage`**（对外）：LLM 只懂三种——`user`、`assistant`、`toolResult`。

中间的**转换管道**，每次调模型前执行，分两步：

```ts
AgentMessage[]
   → transformContext()   // ① 裁剪旧消息、注入外部上下文（可选）
   → convertToLlm()       // ② 过滤 UI 消息、转换类型（必须）
   → LlmMessage[]
```

**为什么分两步？** 因为「裁剪/注入」是**策略**（可以有很多种），「过滤/转换」是**机制**（必须做）。把策略和机制分开，就能自由替换策略而不用动机制。

---

## 三、pi 的实现：两层消息 + Compaction

pi 是这套「两层消息」设计的提出者：

- 内部用 `AgentMessage`，通过**声明合并（declaration merging）**可扩展自定义消息类型。
- 每次调 LLM 前走两步管道：`transformContext()`（裁剪旧消息、注入外部上下文）→ `convertToLlm()`（过滤 UI-only 消息、把自定义类型转成 LLM 格式）。
- **Compaction（压缩）**：手动 `/compact` 或自动触发（上下文溢出时恢复重试、接近上限时主动压缩），把老消息总结掉。
- **关键设计**：压缩是**有损的**，但**完整历史仍留在 JSONL 文件里**，可用 `/tree` 回看。也就是说，压缩只影响「发给模型的内容」，不动磁盘上的原始记录。

---

## 四、DSH 的实现：把上下文管理拆成一堆插件

DSH 不把上下文管理做成一个类，而是拆成多个**可插拔的插件**：

- `dsh-compaction`：压缩的抽象层。
- `dsh-compaction-basic`：通用压缩策略。
- `dsh-compaction-tool-result-pruner`：专门**剪掉旧的工具结果**（工具结果往往又长又占 token，是最值得剪的）。
- `dsh-session-projection`：会话投影（把树形会话投影成线性上下文）。
- `dsh-session-query-sqlite`：用 sqlite 查询历史会话。

**DSH 的启示**：上下文管理不是「一个函数」，而是「**一组可插拔的策略**」。不同的裁剪/压缩策略可以针对不同场景独立演进。

---

## 五、我们如何设计：一个可插拔的 ContextManager

```ts
interface ContextManager {
  // 输入原始 AgentMessage[]，输出喂给模型的 LlmMessage[]
  prepare(messages: AgentMessage[], systemPrompt: string): LlmMessage[]
}

class DefaultContextManager implements ContextManager {
  constructor(private maxTokens: number, private countTokens: (s: string) => number) {}

  prepare(messages: AgentMessage[], systemPrompt: string): LlmMessage[] {
    // ① transformContext：注入系统提示 + 裁剪
    let current = this.injectSystem(messages, systemPrompt)
    current = this.prune(current)

    // ② convertToLlm：过滤 + 类型转换
    return current
      .filter(m => m.role === 'user' || m.role === 'assistant' || m.role === 'tool')
      .map(({ id, parentId, timestamp, kind, ...llm }) => llm)
  }

  private injectSystem(msgs: AgentMessage[], systemPrompt: string): AgentMessage[] {
    // 简化：把系统提示作为一条特殊的 agent 消息塞到最前
    return [{ id: 'system', parentId: null, role: 'assistant', content: systemPrompt, timestamp: 0 }, ...msgs]
  }

  private prune(msgs: AgentMessage[]): AgentMessage[] {
    let total = msgs.reduce((n, m) => n + this.countTokens(m.content), 0)
    const result = [...msgs]
    while (total > this.maxTokens && result.length > 1) {
      total -= this.countTokens(result.shift()!.content)   // 从最旧开始丢
    }
    return result
  }
}
```

**设计要点**：

1. **系统提示不能丢**：裁剪时从最旧的普通消息开始丢，系统提示通常要保留（或单独处理）。
2. **工具结果是重点裁剪对象**：DSH 专门的 `tool-result-pruner` 说明，工具结果又长又占 token，应该优先剪。
3. **压缩 vs 丢弃**：上面的 `prune` 是「丢弃」（简单粗暴）。更高级的做法是「压缩」（用一次 LLM 调用把旧消息总结成摘要），pi 的 `/compact` 就是后者。

---

## 六、设计原因与考虑维度

**为什么「原始历史必须保留」？** 因为压缩/裁剪是有损的，一旦模型判断失误或用户想追溯，原始历史是唯一的真相来源。这也是 pi 和 DSH 都坚持「压缩只管内存上下文、不动磁盘记录」的原因。

**设计上下文管理要考虑的维度**：

| 维度 | 要回答的问题 |
|---|---|
| token 估算 | 怎么算 token？按字符？按分词器？误差多大能接受 |
| 裁剪策略 | 丢最旧的？丢工具结果？还是 LLM 总结压缩？ |
| 注入内容 | 系统提示、当前时间、工作目录、环境信息……什么该注入 |
| 压缩触发 | 手动？超限自动？接近上限主动？ |
| 保留 vs 可追溯 | 压缩掉的内容能否回看（关联到会话存储） |
| 类型转换 | 自定义消息类型如何映射到 LLM 类型 |

---

## 七、达成什么目标

读完本章，你应该能：

- ✅ 解释「两层消息模型」的价值（内部丰富、对外精简）
- ✅ 说明 `transformContext` 和 `convertToLlm` 的分工
- ✅ 理解 pi 的 Compaction 和 DSH 的 tool-result-pruner
- ✅ 写一个带裁剪和转换的 ContextManager
- ✅ 说出上下文管理的 6 个考虑维度

---

## 八、实操练习

1. **实现 `countTokens`**：用一个简单的估算（如「每 4 个字符 ≈ 1 token」），跑通 `prune`。
2. **加一个压缩策略**：在 `prune` 之前，先尝试把最旧的 5 条消息用一次 LLM 调用总结成 1 条摘要，再裁剪。观察 token 变化。
3. **思考题**：系统提示、工具结果、用户消息、助手消息，如果必须裁剪，你的优先级是什么？为什么？（提示：参考 DSH 为什么专门做 tool-result-pruner）

---

> 下一章：[第 3 章 Agent 循环](03-Agent循环.md) —— 把上下文、模型、工具串起来的核心状态机。
