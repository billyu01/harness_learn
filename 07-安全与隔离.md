# 第 7 章 安全与隔离（Security & Isolation）

> 上一章：[第 6 章 扩展机制](06-扩展机制.md) ｜ 下一章：[第 8 章 交互形态](08-交互形态.md)

---

## 一、概念：agent 是「拿着你权限的代码执行器」

一个编码 agent 会**执行任意 shell 命令、读写文件、访问网络**。这意味着：如果它出错、或者被恶意提示词（prompt injection）诱导，它可能：

- 删除文件、泄露密钥
- 把敏感数据外传
- 修改生产配置

**安全与隔离要解决的问题**：怎么让 agent 能干活的**同时**，不搞坏系统、不越权、不出事故。

---

## 二、原理：协作式信任 vs 强边界

先建立最重要的认知——**两种安全范式**：

| 范式 | 含义 | 代表 |
|---|---|---|
| 协作式信任 | 靠「策略 + 审批」约束 agent，相信它配合，但不设硬边界 | DSH 内置层 |
| 强边界 | 用 OS/容器/微VM 硬隔离，agent 越界直接失败 | 外部容器/沙箱 |

**关键真相**：LLM 不是可证明安全的程序。任何「靠提示词/策略」的约束，都只是**协作式信任**——agent 想绕过总能绕过（比如诱导用户、利用工具组合）。真正的强边界必须落在**操作系统层面**（容器、微VM、权限降级）。

---

## 三、pi 的实现：不内置权限，靠外部容器

pi 明确表示：**没有内置权限系统**，默认以启动它的用户权限运行，不限制文件系统/进程/网络/凭据访问。

官方给出的隔离方案是三种容器化：

- **Gondolin 扩展**：host 上跑 pi 和 provider 鉴权，把内置工具和 `!` 命令路由进本地 Linux 微VM。
- **Docker**：整个 pi 进程跑在容器里，简单隔离。
- **OpenShell**：跑在策略控制的沙箱里。

**pi 的哲学**：权限系统很复杂，与其内置一个「自以为安全」的软约束，不如坦诚「我没有」，让用户用成熟的外部隔离方案。这是「极简」哲学的延伸。

---

## 四、DSH 的实现：内置一整套安全插件

DSH 比 pi 严格得多，有一整套安全插件：

| 插件 | 职责 |
|---|---|
| `dsh-sandbox` / `dsh-sandbox-local` | 沙箱执行 |
| `dsh-sandbox-policy` | 文件写入策略（如只允许写 workspace） |
| `dsh-fs-sandbox` | 围住 `ctx.fs` 的写入 |
| `dsh-fs-observation-policy` | 文件观察策略 |
| `dsh-permission-presets` | 权限预设 |
| `dsh-user-approval` | 用户审批 |
| `dsh-authorization` | 授权抽象 |

**DSH 的启示**：安全是「**多层**」的——沙箱（隔离执行环境）+ 策略（允许做什么）+ 审批（危险操作问人）+ 预设（一键配置不同风险等级）。但即便这样，DSH 也强调：**沙箱隔离全局变量，但不是安全边界**，要当 bash 访问对待。

---

## 五、我们如何设计：权限门 + 审批

```ts
interface PermissionRule {
  match: (tool: string, args: any) => boolean       // 命中则需处理
  decision: (tool: string, args: any) => 'allow' | 'deny' | 'ask'
}

class PermissionGate {
  constructor(private rules: PermissionRule[], private ask: (q: string) => Promise<boolean>) {}

  async check(tool: string, args: any) {
    for (const rule of this.rules) {
      if (!rule.match(tool, args)) continue
      const d = rule.decision(tool, args)
      if (d === 'deny') throw new Error(`blocked: ${tool}`)
      if (d === 'ask') {
        const ok = await this.ask(`允许执行 ${tool} ${JSON.stringify(args)} 吗？`)
        if (!ok) throw new Error(`declined: ${tool}`)
      }
    }
  }
}

// 示例策略
const gate = new PermissionGate([
  { match: t => t === 'bash', decision: (t, a) => /rm\s+-rf/.test(a.cmd ?? '') ? 'ask' : 'allow' },
  { match: t => t === 'write', decision: (t, a) => isOutsideWorkspace(a.path) ? 'deny' : 'allow' },
], askUser)
```

**几个关键设计决策**：

1. **权限门夹在 `ToolRegistry.run()` 里**（第 4 章），所有工具执行必经此门——安全是「默认拒绝」，而不是「各工具自己小心」。
2. **`ask` 是「审批」**：危险操作先问人。DSH 的 `dsh-user-approval` 就是干这个。
3. **策略可组合**：多个 rule 按顺序匹配，deny 优先（白名单思维）。

---

## 六、设计原因与考虑维度

**为什么「软约束」不够？** 因为 LLM 会「社会工程」：它可能写一段看似无害的脚本，诱导用户同意，实际执行危险操作。真正的安全边界必须是「**即使 agent 完全恶意，也做不了坏事**」——这只能靠 OS 级隔离（容器、微VM、最小权限用户）。

**设计安全层要考虑的维度**：

| 维度 | 要回答的问题 |
|---|---|
| 隔离层级 | 进程？容器？微VM？各自成本与强度 |
| 权限模型 | 白名单还是黑名单？默认拒绝还是默认允许？ |
| 文件边界 | 允许写哪里？workspace 之外一律拒绝？ |
| 网络 | 允许访问外网吗？哪些域名？ |
| 审批 | 哪些操作必须人工确认？超时怎么办？ |
| 凭据 | 密钥如何隔离，不被 agent 读取或外传 |
| 可回滚 | 危险操作能否撤销（git 检查点、快照） |

---

## 七、达成什么目标

读完本章，你应该能：

- ✅ 区分「协作式信任」和「强边界」两种安全范式
- ✅ 说明 pi「不内置权限」和 DSH「多层安全插件」的取舍
- ✅ 写一个权限门 + 审批的 PermissionGate，接入工具执行
- ✅ 说出安全层的 7 个考虑维度

---

## 八、实操练习

1. **接权限门**：把第 4 章的 `ToolRegistry.run()` 改成执行前先过 `PermissionGate.check()`。
2. **加 workspace 边界**：写一个 rule，让 `write`/`bash` 只能操作 `workingDir` 之内，越界直接 deny。
3. **思考题**：如果 agent 需要「临时访问 workspace 之外的一个文件」，你会用 `deny` 还是 `ask`？如何防止它借这个口子持续越权？

---

> 下一章：[第 8 章 交互形态](08-交互形态.md) —— 一个运行时，如何在终端、浏览器、CI 里「多副面孔」。
