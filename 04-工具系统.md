# 第 4 章 工具系统（Tool System）

> 上一章：[第 3 章 Agent 循环](03-Agent循环.md) ｜ 下一章：[第 5 章 会话与分支](05-会话与分支.md)

---

## 一、概念：让模型「真正能做事」

LLM 只会生成文本。要让它读文件、跑命令、查数据库、发请求，就必须给它「工具」。工具是 agent 与外部世界交互的**唯一通道**。

一个工具的完整定义，包含四要素：

| 要素 | 作用 |
|---|---|
| `name` | 工具名，模型用它来发起调用 |
| `description` | 描述，告诉模型「什么时候该用这个工具」 |
| `parameters` | JSON Schema，告诉模型「调用时传什么参数」 |
| `execute` | 执行函数，真正干活的代码 |

**关键洞察**：`name + description + parameters` 决定**模型怎么调用**（它们会被传给模型），而 `execute` 决定**实际做什么**。两者是分离的——模型只看到 schema，看不到执行逻辑。

---

## 二、原理：Schema 是「契约」，执行是「实现」

模型调用工具的机制，本质是：模型根据 schema 生成一个结构化调用（`name + arguments`），harness 拿到后查表执行 `execute`，把结果回填。

```
模型看到：tools = [{ name, description, parameters }]
模型生成：tool_call = { name: "read", arguments: { path: "a.txt" } }
harness 执行：tools.run("read", { path: "a.txt" })  →  返回文件内容
回填：messages.push({ role: "tool", content: 结果, toolCallId })
```

所以工具系统的核心是一个**注册表（Registry）**：名字 → 工具定义。它负责三件事：

1. `register`：注册工具（扩展机制的基础，第 6 章）。
2. `list`：把工具的 schema 部分提取出来，喂给模型。
3. `run`：根据名字查表执行，**夹一层权限检查**（第 7 章）。

---

## 三、pi 的实现：内置极简工具 + 扩展注册

pi 的哲学是「默认极简」。默认只给模型 4 个工具：

- `read`：读文件
- `write`：创建/覆盖文件
- `edit`：补丁式修改文件
- `bash`：跑 shell 命令

另有只读工具（`grep`、`find`、`ls`）可通过工具选项开启。新的工具通过**扩展**的 `pi.registerTool()` 注册（第 6 章）。

**pi 的启示**：工具不是越多越好。给模型太多工具反而会让它选错。默认少而精，按需加，是更好的起点。

---

## 四、DSH 的实现：工具是插件

DSH 有几十个工具包，且**每个工具都是一个 Cordis 插件**，通过 `inject: ['tools']` 注册：

| 工具包 | 功能 |
|---|---|
| `dsh-tool-bash` / `dsh-tool-pwsh` | shell 执行 |
| `dsh-tool-fs` / `dsh-tool-fs-search` | 文件系统 / 搜索 |
| `dsh-tool-web` | 网络请求 / 搜索 |
| `dsh-tool-subagent` | 子代理 |
| `dsh-tool-workflow` | 多代理编排 |
| `dsh-tool-goal` | 长期目标 |
| `dsh-tool-skill` | 技能加载 |
| `dsh-tool-todo` | 任务清单 |
| `dsh-tool-ask-user` | 向用户提问 |
| `dsh-tool-cordis` | 自指工具集（第 6 章） |

**DSH 的启示**：工具 = 插件。工具的定义、schema、权限、UI 呈现，全部插件化。这让工具可以像插件一样动态加载、卸载、热更新。

---

## 五、我们如何设计：可注册的工具注册表

```ts
interface Tool {
  name: string
  description: string
  parameters: Record<string, unknown>          // JSON Schema
  execute: (args: any, env: ToolEnv) => Promise<any>
}

interface ToolEnv {
  workingDir: string
  signal: AbortSignal                          // 用于取消
}

class ToolRegistry {
  private tools = new Map<string, Tool>()

  register(t: Tool) { this.tools.set(t.name, t) }
  unregister(name: string) { this.tools.delete(name) }

  // 喂给模型的：只要 schema，不要 execute
  list(): ToolDef[] {
    return [...this.tools.values()].map(({ name, description, parameters }) =>
      ({ name, description, parameters }))
  }

  // 实际执行：查表 + 权限检查
  async run(name: string, args: any, env: ToolEnv) {
    const tool = this.tools.get(name)
    if (!tool) throw new Error(`unknown tool: ${name}`)
    await permissionGate.check(name, args)      // 权限门（第 7 章）
    return tool.execute(args, env)
  }
}

// 内置工具示例
const readTool: Tool = {
  name: 'read',
  description: '读取文件内容',
  parameters: { type: 'object', properties: { path: { type: 'string' } }, required: ['path'] },
  execute: async ({ path }, env) => fs.readFile(resolve(env.workingDir, path), 'utf8'),
}
```

**几个关键设计决策**：

1. **`list()` 和 `run()` 分离**：`list()` 给模型看 schema，`run()` 执行逻辑，两者绝不混在一起——`execute` 永远不会泄露给模型。
2. **`ToolEnv` 注入运行环境**：`workingDir`、`signal` 等环境信息通过参数注入，而不是让工具自己去读全局变量。这让工具可测试、可沙箱化。
3. **权限检查夹在 `run()` 里**：所有工具执行必经此门，安全层（第 7 章）只改这一处。

---

## 六、方案设计

### 6.1 验收标准

工具系统「做好」有两条可验证的标准：

1. **模型用得对**：description 和 schema 足够清晰，模型在应该用工具时用对工具、传对参数。
2. **执行受控**：每个工具执行前必经权限门，执行环境和结果都受约束——参数错不了、越权做不了、结果撑不爆上下文。

第一条关乎「模型能不能驾驭工具」，第二条关乎「工具会不会闯祸」。

### 6.2 关键取舍一：给模型多少工具

这是工具系统最容易被忽视、却最影响效果的决策。

| | 工具太多 | 工具太少 |
|---|---|---|
| 效果 | schema 塞满上下文，token 涨，模型选错 | agent 做不了事 |
| 原因 | 模型在几十个工具里挑，选择困难 | 没有对应工具只能硬答 |

**立场：默认少而精，按需增加。** pi 默认 4 个（read/write/edit/bash）、DSH 按 profile 组合，本质都是「按需给工具」。**每增加一个工具，都要问：它解决的真实任务是什么？没有它 agent 会卡住吗？** 如果答案模糊，就不加。

**失效条件**：做「通用 agent 平台」时，工具集天然要全（因为不知道用户会派什么活）。这时用「工具分组 / 按 profile 激活」来控制「**当前会话实际暴露给模型的工具数**」，而不是一次性全塞。

### 6.3 关键取舍二：参数校验——交给谁

模型传的参数可能类型错、缺字段、甚至格式非法。校验放哪：

- **不校验**：直接塞给 `execute`，错了就 crash。
- **schema 校验**：用 JSON Schema / TypeBox / Zod 在执行前校验。

**立场：执行前必须校验，且校验错误要「回传给模型」而不是 crash。** 理由：模型调用参数错误是**常态**（不是异常）。如果校验失败就抛异常，会话就断了；如果返回一条结构化错误「参数 `path` 缺失」，模型能**立刻修正重试**。这比崩溃后人工介入好得多。

**失效条件**：无。参数校验是工具系统的地基，不是可选项。

### 6.4 关键取舍三：工具结果的尺寸边界

工具可能返回超长结果（`npm test` 输出几千 token、读一个巨型文件）。这些结果回填进上下文（第 2 章）会撑爆窗口。

**立场：在工具层就截断，而不是等上下文层兜底。** 理由：上下文层的裁剪是「事后补救」（结果已经进入历史），工具层的截断是「事前预防」（脏数据根本不产生）。两者都要有，但工具层先做。

具体策略：给工具结果设一个 token 上限（如 2000），超了就**保留头部 + 尾部，中间标注「省略 N 字符」**，并告诉模型「结果被截断了，如需完整内容请用更精确的查询」。这比「只留头部」好，因为尾部往往含关键结论。

### 6.5 落地方案

- **第一步（地基）**：`Tool` 四要素 + `ToolRegistry`（`register/list/run`），`list()` 只暴露 schema、`run()` 才执行。
- **第二步**：在 `run()` 里加 schema 参数校验，校验失败返回结构化错误给模型。
- **第三步**：加结果截断（token 上限 + 头尾保留）。
- **第四步（对接）**：把权限门（第 7 章）接入 `run()`，让所有工具执行必经检查。

关键顺序：**先让「工具能注册、能执行」，再让「参数对、结果不爆」，最后让「越权做不了」。** 权限门可以稍后接，但 `run()` 里要预留这个挂载点（这正是第 4 章代码里 `permissionGate.check` 占位的原因）。

### 6.6 反模式（避坑）

1. **❌ description 写得含糊（如「处理数据」）**——结果：模型不知道何时用它，宁可不用。正确做法：description 写清「什么时候用、输入什么、返回什么」。
2. **❌ 参数校验失败直接 throw**——结果：一次参数错误就中断整个会话。正确做法：返回结构化错误让模型修正。
3. **❌ 工具结果不截断直接回填**——结果：一条巨型结果撑爆上下文，拖垮后续所有轮次。正确做法：工具层就截断。
4. **❌ 把 `execute` 的实现细节暴露给模型**——结果：模型可能「学会」绕过工具直接操作内部。正确做法：`list()` 只给 schema，`execute` 永远不出现在上下文里。

---

## 七、达成什么目标

读完本章，你应该能：

- ✅ 说出工具的「四要素」，并解释 schema 与 execute 的分离
- ✅ 理解 pi 的「默认极简」和 DSH 的「工具插件化」
- ✅ 写一个可注册、带权限门、注入环境的 ToolRegistry
- ✅ 复述「默认少而精」「参数校验回传」「结果截断」三个关键取舍

---

## 八、实操练习

1. **写 3 个工具**：`read`、`write`、`bash`，接入第 3 章的循环，跑通「让模型读文件并回答内容」。
2. **加参数校验**：给 `run()` 加一个「用 schema 校验 args」的步骤，传错参数时返回清晰的错误给模型（而不是 crash）。
3. **思考题**：工具返回的结果如果长达 10 万 token，会不会撑爆上下文？你会在哪里截断？（提示：关联第 2 章的上下文管理）

---

> 下一章：[第 5 章 会话与分支](05-会话与分支.md) —— 让 agent「记得住、能回退、可探索」。
