# 第 9 章 观测与遥测（Observability & Telemetry）

> 上一章：[第 8 章 交互形态](08-交互形态.md) ｜ 下一章：[第 10 章 范式总结与对比](10-范式总结与对比.md)

---

## 一、概念：不观测，就无法优化

一个 agent 跑完一个任务，你需要知道：

- 它**花了多少钱**（token、费用）
- 它**每一步做了什么**（调了哪个工具、结果如何、想了什么）
- 它**为什么失败了**（哪个工具出错、哪一轮卡住）
- 它**效率如何**（有没有反复调同一个工具、有没有走弯路）

**观测与遥测要解决的问题**：把 agent 运行过程中的关键信息，系统地记录下来，用于成本控制、性能优化和故障诊断。

---

## 二、原理：轨迹（Trajectory）是核心数据

Agent 的观测，核心是**轨迹（trajectory）**——agent 从开始到结束的完整「思考 + 行动」序列：

```
[llm_call]  输入 1234 token，输出 56 token，耗时 2.1s，成本 $0.003
[tool_exec] read("a.txt") → 成功，耗时 3ms
[llm_call]  输入 1500 token，输出 300 token，耗时 5.0s
[tool_exec] bash("npm test") → 成功，耗时 42s
[llm_call]  ... → 最终回答
[done]  reason: stop，总 token: 8900，总成本: $0.041
```

有了轨迹，你就能：

- **成本归因**：哪一步最烧钱？
- **性能定位**：哪个工具最慢？
- **行为诊断**：模型为什么反复调用同一工具？
- **回放复现**：完整重放 agent 的决策过程。

---

## 三、pi 的实现：内置 token/成本追踪

pi 内置了 token/成本追踪，在终端 footer 实时显示：

- `↑` 输入 token、`↓` 输出 token
- `R` cache 读、`W` cache 写、`CH` 缓存命中率
- 成本、上下文用量、当前模型

pi 的 `pi-ai` 在模型层就统一了 token/费用统计，所以每次调用都能拿到用量和成本。

---

## 四、DSH 的实现：观测插件化

DSH 把观测拆成多个插件：

| 插件 | 职责 |
|---|---|
| `dsh-token-meter` | token 计量 |
| `dsh-session-telemetry-otel` | OpenTelemetry 导出（对接标准监控栈） |
| `dsh-session-telemetry` | 遥测抽象 |
| `dsh-session-stats` | 会话统计 |
| `dsh-client-ui-trajectory` | 轨迹可视化（Web UI 里展示） |
| `dsh-output-retention` | 输出保留策略 |

**DSH 的启示**：观测是「**贯穿每一次 LLM 调用和每一次工具执行**」的横切关注点，且要能对接标准协议（OpenTelemetry），而不是自己造一套孤立的监控。

---

## 五、我们如何设计：Tracer + Span

```ts
interface Span {
  type: string                     // llm_call / tool_exec / ...
  start: number
  end: number
  meta: Record<string, unknown>    // token、成本、工具名、参数等
}

class Tracer {
  private spans: Span[] = []

  trace<T>(type: string, fn: () => Promise<T>, meta: Record<string, unknown> = {}): Promise<T> {
    const start = Date.now()
    return fn().then(r => {
      this.spans.push({ type, start, end: Date.now(), meta })
      return r
    }).catch(e => {
      this.spans.push({ type, start, end: Date.now(), meta: { ...meta, error: String(e) } })
      throw e
    })
  }

  export(): Span[] { return this.spans }
}
```

在核心循环里埋点：

```ts
// 模型调用
const response = await this.tracer.trace('llm_call',
  () => this.model.chat(...),
  { tokens: estimate(messages) },
)

// 工具执行
const result = await this.tracer.trace('tool_exec',
  () => this.tools.run(call.name, call.arguments),
  { tool: call.name, args: call.arguments },
)
```

**几个关键设计决策**：

1. **`trace` 包裹而非散落打点**：把观测点集中在「调用边界」，代码不侵入业务逻辑。
2. **`meta` 记录上下文**：token、工具名、参数、错误——这些都是诊断的关键字段。
3. **`export()` 可对接任何后端**：本地文件、OpenTelemetry、控制台，取决于你怎么消费 spans。

---

## 六、方案设计

### 6.1 验收标准

观测层「做好」有两条可验证的标准：

1. **可归因**：任何一笔 token/费用/耗时，都能追溯到「哪个会话、哪个工具调用、哪一步」。
2. **可回放**：拿到轨迹，能完整重演 agent 从开始到结束的决策过程。

第一条关乎「成本与性能」，第二条关乎「诊断与调试」。两者都指向同一个核心：**轨迹（trajectory）必须是结构化、可回放的，而不是散落的日志行**。

### 6.2 关键取舍一：埋点粒度——只记 LLM 调用，还是全量

观测埋到哪里，是「信息量」和「噪音/成本」的平衡：

- **只记 LLM 调用**：轻，但看不到工具内部的细节。
- **全量**（LLM + 每个工具 + 每个事件）：信息全，但噪音大、存储贵。

**立场：以「LLM 调用 + 工具执行」为最小埋点，事件级按需加。** 理由：这两处是「成本和行为的全部来源」——token 花在 LLM 调用，耗时花在工具执行。只埋这两点，就能满足「可归因」；而「每个事件都记」带来的边际信息（中间状态）远小于边际成本（存储 + 噪音）。

**失效条件**：调试「为什么卡在这一步」时，需要更细的事件级轨迹。此时把事件级埋点做成**可开关的 debug 级别**，默认关，需要时开。

### 6.3 关键取舍二：轨迹存哪、留多久

轨迹（含 agent 读过的所有源代码）是敏感数据，存储要有边界。

**立场：分层存储 + 明确保留期。** 具体：

- **热轨迹**（内存）：当前会话的完整轨迹，用于实时展示和调试。
- **冷轨迹**（落盘/远端）：只保留「可归因」所需的最小字段（会话 id、时间、token、成本、工具名），**源码等敏感内容默认不落冷存储**，除非显式开启。

**理由**：完整轨迹含源代码，长期存是隐私和合规的雷（第 7 章安全 + 合规）。而「可归因」只需要聚合后的元数据，不需要原文。**把「诊断用的完整轨迹」和「归因用的聚合指标」分开，是观测层的关键设计**。

### 6.4 关键取舍三：导出协议——自建还是 OpenTelemetry

观测数据给谁消费：

- **自建格式**：灵活，但要自己写所有消费端（面板、告警、分析）。
- **OpenTelemetry**：标准协议，对接现成的监控栈（Prometheus、Jaeger、Grafana）。

**立场：数据模型自建（因为 agent 轨迹是领域特有的），导出对接 OTel。** 理由：agent 轨迹的「多轮、树形、有工具调用」结构，OTel 的 span 模型能表达但别扭，所以内部用自己的 `Tracer` + `Span`。但对外导出时，用 OTel 协议能白嫖一整套成熟监控生态——DSH 正是这么做的（`dsh-session-telemetry-otel`）。

**失效条件**：纯内部小工具，自建格式 + 控制台打印就够了，OTel 是「要接监控栈」时才需要。

### 6.5 落地方案

- **第一步（地基）**：`Tracer` + `Span`，在循环的「模型调用」「工具执行」两处埋点。此时「可归因」初步达标。
- **第二步**：把 spans 导出成 JSONL 落盘，支持事后回放分析。
- **第三步（分拆）**：把「完整轨迹（诊断）」和「聚合指标（归因）」分开存储，敏感内容不进冷存储。
- **第四步（进阶）**：对接 OTel，接 Prometheus/Grafana；做轨迹可视化。

关键顺序：**先「能埋点、能归因」，再「分拆敏感数据」，最后「对接监控栈」。** 埋点是最早也最重要的一步——没有埋点，后面全无源之水。

### 6.6 反模式（避坑）

1. **❌ 用散落的 `console.log` 代替结构化轨迹**——结果：无法回放、无法归因、无法分析。正确做法：结构化 Span。
2. **❌ 完整轨迹（含源码）长期全量落盘**——结果：隐私合规的雷。正确做法：诊断轨迹和聚合指标分开，敏感内容按需保留。
3. **❌ 埋点散落在业务代码各处**——结果：漏埋、重复埋，改业务还要改观测。正确做法：用 `tracer.trace()` 包裹调用边界，不侵入业务。
4. **❌ 一开始就上 OTel + Grafana 全套**——结果：为「观测」搭了套监控系统，却还没想清楚要观测什么。正确做法：先埋点看数据，再决定要不要接监控栈。

---

## 七、达成什么目标

读完本章，你应该能：

- ✅ 解释「轨迹」为什么是 agent 观测的核心数据
- ✅ 说明 pi 的内置追踪和 DSH 的观测插件化
- ✅ 写一个 Tracer，在 LLM 调用和工具执行处埋点
- ✅ 复述「最小埋点」「诊断轨迹 vs 聚合指标分离」「自建模型 + OTel 导出」三个关键取舍

---

## 八、实操练习

1. **埋点**：给第 3 章的循环的「模型调用」和「工具执行」两处都包上 `tracer.trace`。
2. **成本归因**：统计一次任务里，每个工具调用花了多少 token/耗时，找出最烧钱的环节。
3. **思考题**：轨迹里包含 agent 读过的所有源代码。如果这份轨迹要分享给团队（pi 鼓励分享 OSS session），你如何脱敏？（提示：关联第 7 章的安全）

---

> 下一章：[第 10 章 范式总结与对比](10-范式总结与对比.md) —— 把九大模块串起来，看 pi 与 DSH 的两种哲学。
