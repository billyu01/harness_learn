# 第 5 章 会话与分支（Session & Branching）

> 上一章：[第 4 章 工具系统](04-工具系统.md) ｜ 下一章：[第 6 章 扩展机制](06-扩展机制.md)

---

## 一、概念：会话要「记得住、能回退、可探索」

一个 agent 干活的中间状态，如果断电就丢，那它就不是一个可靠的智能体。会话管理要解决三个问题：

1. **持久化**：会话存到哪，怎么存？
2. **恢复**：下次启动怎么接上？
3. **分支**：用户想「回到之前某个点重新来」，或「从某个点探索另一条路」，怎么办？

**「分支」是 agent harness 区别于普通聊天机器人（如 ChatGPT 对话列表）的关键能力**——它把会话从「一条直线」变成「一棵树」，让你能探索不同路径而不丢失历史。

---

## 二、原理：树形结构 + JSONL 追加写

最优雅的方案是：**每条消息带 `id` 和 `parentId`，构成一棵树**。

```ts
interface Entry {
  id: string
  parentId: string | null   // 指向父消息，null 表示根
  role: string
  content: string
  timestamp: number
}
```

- **追加写（append-only）**：每来一条消息，就往文件末尾追加一行 JSONL。不修改历史，天然支持「回到过去」。
- **`parentId` 构成树**：要重建某条分支的线性历史，只需沿 `parentId` 向上追溯。
- **分支零成本**：从某个历史点分叉，就是在那个点的 `id` 下新挂一个子节点，不需要复制整个会话。

```ts
// 存储：JSONL 文件
// 每行一条消息，按时间顺序追加
{"id":"1","parentId":null,"role":"user","content":"写个函数"}
{"id":"2","parentId":"1","role":"assistant","content":"好的...","toolCalls":[...]}
{"id":"3","parentId":"2","role":"tool","content":"结果","toolCallId":"..."}
// 从这里分叉：回到 id=2，重新回答
{"id":"4","parentId":"2","role":"assistant","content":"换个思路..."}
```

---

## 三、pi 的实现：JSONL 树 + 原地分支

pi 把会话存为 **JSONL 文件，树结构**。每条 entry 有 `id` 和 `parentId`，所以能在**原地分支**：

- `/tree`：可视化会话树，跳转到任意点继续。
- `/fork`：从某个历史 user 消息创建新会话文件。
- `/clone`：复制当前活动分支到新会话。
- `--fork <path|id>`：CLI 直接分叉。

**关键点**：所有历史保留在**单个 JSONL 文件**里，树的分支信息全靠 `id/parentId` 编码，不靠文件名。这比「每个分支一个文件」简洁得多。

---

## 四、DSH 的实现：会话是一整套插件

DSH 把会话拆成一组插件，每个插件管一个子问题：

| 插件 | 职责 |
|---|---|
| `dsh-session` | 会话核心抽象 |
| `dsh-session-persistence-jsonl` | JSONL 持久化后端（另有无独立的 `dsh-session-persistence-sqlite` SQLite 后端） |
| `dsh-session-persistence` | 持久化抽象层 |
| `dsh-session-checkpoint-policy` | 检查点策略（何时落盘） |
| `dsh-session-title-llm` | 用 LLM 自动给会话起标题 |
| `dsh-session-projection` | 把树形会话投影成线性上下文 |
| `dsh-session-query-sqlite` | 用 sqlite 查询历史 |
| `dsh-session-log-export` | 导出会话日志 |

**DSH 的启示**：会话管理不是「一个文件读写函数」，而是「持久化 + 检查点 + 投影 + 查询 + 标题 + 导出」的一组正交能力，各自插件化。

---

## 五、我们如何设计：一个 JSONL 树形存储

```ts
interface SessionStore {
  append(msg: AgentMessage): Promise<void>
  load(): Promise<AgentMessage[]>                       // 读全部
  ancestors(id: string): Promise<AgentMessage[]>         // 沿 parentId 回溯到根
  children(id: string): Promise<AgentMessage[]>          // 分支点
}

class JsonlSessionStore implements SessionStore {
  constructor(private file: string) {}

  async append(msg: AgentMessage) {
    await fs.appendFile(this.file, JSON.stringify(msg) + '\n')
  }

  async load(): Promise<AgentMessage[]> {
    const text = await fs.readFile(this.file, 'utf8')
    return text.split('\n').filter(Boolean).map(l => JSON.parse(l))
  }

  async ancestors(id: string): Promise<AgentMessage[]> {
    const all = await this.load()
    const byId = new Map(all.map(m => [m.id, m]))
    const path: AgentMessage[] = []
    let cur = byId.get(id)
    while (cur) {
      path.unshift(cur)
      cur = cur.parentId ? byId.get(cur.parentId) : undefined
    }
    return path
  }

  async children(id: string): Promise<AgentMessage[]> {
    const all = await this.load()
    return all.filter(m => m.parentId === id)
  }
}
```

**几个关键设计决策**：

1. **追加写而非覆盖**：历史不可变，天然可回退、可审计。
2. **`ancestors()` 是核心**：把树里的一条路径还原成线性历史，供上下文管理（第 2 章）使用。
3. **`children()` 支持分支**：一个父节点下多个子节点 = 多个分支。

---

## 六、方案设计

### 6.1 验收标准

会话管理「做好」有两条可验证的标准：

1. **不丢不坏**：进程崩溃、断电，历史消息不丢、格式不坏（能恢复到崩溃前最后一条完整消息）。
2. **可回溯可分支**：用户能回到任意历史点继续、能分叉探索，且不破坏已有分支。

第一条关乎「可靠性」，第二条关乎「探索能力」。两条都指向同一个设计选择：**append-only 的树形结构**。

### 6.2 关键取舍一：存储形态——JSONL / SQLite / 多文件

| | 单文件线性 | 多文件（每分支一文件） | 树形（id/parentId） |
|---|---|---|---|
| 分支 | ❌ 无法探索 | 要复制历史，浪费 | ✅ 零复制 |
| 回溯 | 只能看当前 | 难以跨分支对比 | ✅ 沿 parentId 回溯 |
| 复杂度 | 低 | 中 | 中（但要维护 id 关系） |

**立场：树形（id/parentId），这是 agent harness 的成熟方案。** 理由：树形把「分支」和「回溯」统一到一个数据结构里——`parentId` 就是分支指针，`ancestors(id)` 就是回溯。多文件做不到跨分支对比，单文件线性做不到探索。

**失效条件**：如果你只做「一次性问答、不需要回退和分支」（比如纯批处理），单文件线性更简单。但 agent 的「探索不同路径」是核心价值，放弃分支等于砍掉一半能力。

### 6.3 关键取舍二：存储后端——JSONL vs SQLite

确定了树形结构，还要选物理存储：

- **JSONL（append-only）**：每条消息追加一行。简单、可审计、零依赖、崩溃安全（天然 append-only）。
- **SQLite**：结构化查询快（按会话/时间/关键词查），但要维护表结构、索引。

**立场：默认 JSONL，SQLite 作为「查询加速」而非「存储真相」。** 理由：JSONL 的 append-only 特性天然满足「不丢不坏」（崩溃最多丢最后半行，前面都完好），而且可读、可导出、可审计。SQLite 的优势是查询，但那是「读」的需求，不是「存」的需求。

DSH 的实践印证了这点：`dsh-session-persistence-jsonl` 是持久化后端，`dsh-session-query-sqlite` 是**独立的查询层**（用 SQLite FTS5 做全文搜索）——两者分离，而不是「用 SQLite 代替 JSONL 存储」。

**失效条件**：多租户、海量会话、需要复杂 SQL 查询（第 7 章的「多租户」场景）时，才考虑 SQLite/Postgres 直接作为存储。但即便那时，也建议「主存储 + 查询索引」分离，而不是让存储和查询耦合。

### 6.4 关键取舍三：检查点——每条落盘 vs 批量

会话消息什么时候写入磁盘：

- **每条落盘**：每来一条消息就 append。最安全，但磁盘 IO 频繁。
- **批量**：攒一批再写。省 IO，但崩溃时丢最近一批。

**立场：每条落盘（append-only），批量只作为「写入合并」的优化，不改变「每条消息都持久化」的语义。** 理由：agent 单条消息的生成间隔本来就不密集（要等模型/工具），append 的 IO 开销可以忽略，换来的是「崩溃不丢消息」的强保证。DSH 的 `dsh-session-persistence` 里有「写批处理」（bounded batching），但那是**延迟写入以合并 IO**，最终每条消息都会落盘，语义上仍是「每条持久化」。

**失效条件**：极高吞吐（每秒几千条消息）时才需要真正批量 + 定期快照。编码 agent 达不到这个吞吐。

### 6.5 落地方案

- **第一步（地基）**：定义 `Entry { id, parentId, role, content }` + `JsonlSessionStore.append()`，实现 append-only 写入。此时「不丢不坏」达标。
- **第二步**：实现 `ancestors(id)` 和 `children(id)`，让树形结构「可回溯、可分支」。
- **第三步**：加恢复——冷启动时读最后一条完整消息，自动接上。
- **第四步（对接）**：把 `ancestors(id)` 的结果交给上下文层（第 2 章）做投影；需要搜索时再接 SQLite 查询层（独立于存储）。

关键顺序：**先「存得对」（append-only），再「查得到」（ancestors/children），最后「搜得快」（SQLite 查询层）。** 存储和查询分离，是本章最核心的一条原则。

### 6.6 反模式（避坑）

1. **❌ 用「覆盖写」更新历史**——结果：崩溃时文件损坏，且无法审计/回退。正确做法：append-only，历史不可变。
2. **❌ 每个分支存一个文件**——结果：分叉时复制整个历史，浪费空间、难对比。正确做法：单文件树形，靠 parentId 编码分支。
3. **❌ 让 SQLite 既做存储又做查询**——结果：存储和查询耦合，换存储要重写查询。正确做法：JSONL 存真相，SQLite 只做查询索引。
4. **❌ 投影时直接「读全文件遍历」而不做索引**——结果：会话一长，每次投影都全量扫描。正确做法：投影用 `ancestors(id)` 按需回溯，别每次全量。

---

## 七、达成什么目标

读完本章，你应该能：

- ✅ 解释「树形会话」为什么是 harness 的关键能力
- ✅ 用 `id/parentId` 实现 JSONL 树形存储
- ✅ 理解 pi 的 `/tree`/`/fork` 和 DSH 的会话插件套件
- ✅ 复述「树形 id/parentId」「JSONL 存真相 + SQLite 做查询」「每条落盘」三个关键取舍

---

## 八、实操练习

1. **实现 `ancestors` 和 `children`**：跑通「从 id=4 回溯到根」「查 id=2 的所有分支」。
2. **加分叉**：给会话「回到 id=2 重新回答」，验证新消息挂在 id=2 下、老分支不受影响。
3. **思考题**：树形历史投影成线性上下文时，选哪条分支？如果用户在多条分支间切换，上下文怎么重建？（提示：关联第 2 章的 projection）

---

> 下一章：[第 6 章 扩展机制](06-扩展机制.md) —— 框架与脚本的分水岭，Harness 的灵魂。
