# 第 10 章 范式总结与对比

> 上一章：[第 9 章 观测与遥测](09-观测与遥测.md) ｜ 下一章：[第 11 章 从零实现实操](11-从零实现实操.md)

---

## 一、九大模块，一张图

把第 1–9 章串起来，就是一个完整的 Agent Harness：

```
                         ┌─────────────────────────────┐
                         │   观测与遥测（第 9 章）       │  ← 横切：贯穿所有
                         └─────────────────────────────┘
用户输入
   │
   ▼
┌──────────────┐   ┌──────────────┐   ┌──────────────┐
│ 会话与分支    │──▶│ 上下文管理    │──▶│ 模型接入层    │
│ (第 5 章)    │   │ (第 2 章)    │   │ (第 1 章)    │
└──────────────┘   └──────────────┘   └──────┬───────┘
                                              │ 调用
                                              ▼
                                    ┌──────────────┐
                                    │ Agent 循环    │  ← 心脏（第 3 章）
                                    │ (第 3 章)    │
                                    └──────┬───────┘
                                           │ 工具调用
                                           ▼
                                    ┌──────────────┐   ┌──────────────┐
                                    │ 工具系统      │──▶│ 安全与隔离    │
                                    │ (第 4 章)    │   │ (第 7 章)    │
                                    └──────────────┘   └──────────────┘
                                           │
                                           ▼
                                    ┌──────────────┐
                                    │ 交互形态      │  ← CLI/Web/headless/RPC
                                    │ (第 8 章)    │
                                    └──────────────┘
                         ┌─────────────────────────────┐
                         │   扩展机制（第 6 章）         │  ← 灵魂：给一切加能力
                         └─────────────────────────────┘
```

**一句话总结范式**：

> **Agent Harness = 统一模型层 + 事件流运行时 + 可插拔工具 + 树形会话 + 扩展机制 + 隔离 + 观测。**

其中：

- **核心循环**（第 3 章）是心脏
- **扩展机制**（第 6 章）是灵魂
- **事件流**（第 3、8 章）是「一个运行时多副面孔」的关键
- **分支会话**（第 5 章）是区别于聊天机器人的分水岭

---

## 二、pi 与 DSH：同一种范式，两种哲学

| 维度 | pi | DSH |
|---|---|---|
| **哲学** | 极简 + 扩展（默认少，按需加） | 全面 + 插件化（默认全，按需换） |
| **扩展方式** | 轻量 TS 函数（Extension） | 重量级 DI 插件（Cordis） |
| **主界面** | 终端 TUI | Web GUI |
| **安全** | 无内置，靠外部容器 | 内置沙箱 + 权限 + 审批 |
| **会话** | JSONL 树（单文件分支） | 会话插件套件（jsonl/sqlite/投影/查询） |
| **自指能力** | 无（靠扩展） | 有（cordis_define 现场造插件） |
| **模型层** | pi-ai（自研） | 复用 pi-ai + 官方适配 |
| **适合** | 个人 / 轻量 / 终端党 | 团队 / 企业 / 平台化 |

---

## 三、两种哲学的深层解读

### pi：极简主义的胜利

pi 的理念是「**让 pi 适配你的工作流，而不是反过来**」。它默认只给 4 个工具，跳过子代理、plan mode 等重特性，把「加功能」这件事完全交给扩展。

**优点**：核心代码少、易理解、易审计、启动快、上手门槛低。
**代价**：复杂功能（子代理、权限系统、团队协作）要自己搭，或依赖第三方扩展的质量。

### DSH：插件化的力量

DSH 的理念是「**Everything is a Plugin**」。它把一切能力都做成插件，用 Cordis 的依赖注入组织，用 profile/bundles/patch 分层配置。

**优点**：扩展性极强、能承载大型系统、能力可组合替换、适合团队平台化。
**代价**：概念多、上手陡峭、需要理解 DI 容器和 patch 配置。

---

## 四、共同点：这才是范式的本质

无论哲学如何，pi 和 DSH 都共享这 7 个设计决策：

1. **统一的模型层**（第 1 章）——屏蔽厂商差异
2. **事件流驱动的运行时**（第 3 章）——UI 解耦
3. **可插拔的工具系统**（第 4 章）——schema 与执行分离
4. **持久化 + 分支的会话**（第 5 章）——树形历史
5. **扩展机制**（第 6 章）——框架而非脚本
6. **隔离**（第 7 章）——协作式信任 vs 强边界
7. **观测**（第 9 章）——轨迹是核心数据

**这 7 点，就是「Agent Harness 设计范式」的完整定义。** 你理解了它们，就能看懂任何 agent 框架（Claude Code、Aider、AutoGPT……）的设计，也能按需实现自己的。

---

## 五、如何选择：轻量还是重量

给你一个决策建议：

| 你的场景 | 选哪条路 |
|---|---|
| 个人使用、快速验证、终端党 | pi 式（轻量函数扩展） |
| 团队协作、要平台化、要多人定制 | DSH 式（重量 DI 插件） |
| 自己学习、理解原理 | 先轻量（第 11 章 Phase 1–5），再理解重量 |
| 接企业私有模型、多模型路由 | 重点做模型层（第 1 章）+ 安全层（第 7 章） |
| 给现有业务系统加 AI 能力 | 重点做工具系统（第 4 章）+ 扩展机制（第 6 章） |

---

## 六、达成什么目标

读完本章，你应该能：

- ✅ 画出九大模块的完整架构图
- ✅ 用一句话概括 Agent Harness 范式
- ✅ 对比 pi 与 DSH 的两种哲学，并说出各自优劣
- ✅ 根据自己的场景，判断该走轻量还是重量路线

---

> 下一章：[第 11 章 从零实现实操](11-从零实现实操.md) —— 把九大模块拼成一个能跑的 harness。
