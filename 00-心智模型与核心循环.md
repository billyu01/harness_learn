# 第 0 章 心智模型与核心循环

> 上一章：[首页](Agent-Harness设计范式教程.md) ｜ 下一章：[第 1 章 模型接入层](01-模型接入层.md)

---

## 一、概念：Agent Harness 是什么

**Harness**（挽具）这个词很形象。马本身跑得再快，没有挽具、缰绳、车架，它拉不了货、耕不了地。大模型也一样——LLM 能力再强，它只是一次「输入文本、输出文本」的函数调用，什么实际的事情都做不了。

**Agent Harness 就是套在 LLM 身上的那层软件**，它负责把「一个会接话的模型」改造成「一个能自主完成任务的智能体」。

裸 LLM 缺的能力，就是 harness 要补的能力：

| 裸 LLM 不会 | Harness 必须补上 |
|---|---|
| 自己读文件、跑命令 | 工具系统（第 4 章） |
| 记住多轮上下文、在超长对话里做取舍 | 上下文管理（第 2 章） |
| 调用外部工具并处理结果 | 核心循环（本章）+ 工具系统 |
| 持久化会话、支持分支 | 会话与分支（第 5 章） |
| 让用户扩展它的能力 | 扩展机制（第 6 章） |
| 保证不搞坏系统 | 安全与隔离（第 7 章） |
| 在终端/浏览器/CI 里都能用 | 交互形态（第 8 章） |
| 告诉我们它花了多少、做了什么 | 观测与遥测（第 9 章） |

所以这本书的组织方式就是：**先讲心脏（循环），再逐个讲补上的能力，最后回到整体对比。**

---

## 二、原理：核心循环是唯一的「心脏」

无论 pi、DSH，还是你未来自己写的 harness，**它们的核心都是同一个循环**。这个循环只有五步：

```
用户输入
   │
   ▼
① 上下文装配（把历史消息 + 工具定义 + 系统提示组装成一次 LLM 请求）
   │
   ▼
② 调用 LLM（流式）
   │
   ▼
③ 解析输出 ── 是文本？── 是 → 输出给用户，结束
   │
   是工具调用？
   │
   ▼
④ 执行工具（在沙箱里，过权限检查）
   │
   ▼
⑤ 把工具结果回填到上下文 → 回到 ② 继续循环
```

这个循环的本质，是把 LLM 从「一问一答」变成「**一个能持续行动、直到目标达成的过程**」。

- 第 ① 步：**喂给模型什么**（上下文管理）
- 第 ② 步：**怎么调模型**（模型接入层）
- 第 ③ 步：**模型说了什么**（输出解析）
- 第 ④ 步：**让模型真正能做事**（工具系统 + 安全）
- 第 ⑤ 步：**把做事的结果告诉模型，继续想**（回填，循环的关键）

**pi 和 DSH 的所有复杂度，都是在这个五步循环外面层层包裹**：更好的上下文管理、更多的工具、会话持久化、扩展机制、安全、多形态 UI、观测。理解了循环，你就理解了「为什么这些模块存在」。

---

## 三、pi 的实现：一个事件流驱动的循环

pi 把这个循环实现在 `pi-agent-core` 包里，核心是 `Agent` 类 + `prompt()` 方法。它最特别的地方是：**`prompt()` 不返回一个最终字符串，而是广播一串事件**。

```ts
// pi 的事件序列（简化）
agent_start
  → turn_start
  → message_start   { message: userMessage }     // 你的输入
  → message_end
  → message_start   { message: assistantMessage } // 模型开始回
  → message_update  { delta: "流式文本片段" }       // 一个字一个字流出来
  → toolcall_start  { index: 0 }                   // 模型决定调用工具
  → toolcall_delta  { partial: 部分参数JSON }
  → toolcall_end    { toolCall: { name, arguments } }
  → （执行工具，结果回填，继续下一轮 turn）
  → message_start → message_update → ...
  → done            { reason: "stop" }
```

**为什么 pi 要用事件流？** 因为这样，终端 UI、Web UI、headless 模式、SDK，都变成了「事件的订阅者」，而不是各自去实现一遍循环。这就是第 8 章要展开的「一个运行时，多副面孔」。

---

## 四、DSH 的实现：循环本身也被插件化

DSH 的循环和 pi 本质相同，但组织方式不同：**DSH 把循环里的每个环节都做成了插件**。

- `dsh-agent`：agent 的核心
- `dsh-agent-loop`：专门管循环逻辑
- `dsh-tool-call-timeout-policy`：工具调用超时策略（循环里的一个「策略钩子」）
- `dsh-repeat-tool-reminder`：防止模型反复调用同一个工具（又一个钩子）

这体现了一个关键设计理念：**循环里的每个策略（超时、重试、取消、防重复）都应该是可配置、可替换的钩子，而不是写死在循环里。**

pi 的做法更「内聚」（循环写在一个 Agent 类里），DSH 的做法更「分散」（循环拆成多个插件）。两者没有对错，对应的是第 10 章要讲的两种哲学：极简 vs 全面。

---

## 五、我们如何设计：一个最小的循环骨架

理解了原理，最小可用的循环其实很简单。这是全书的「地基」，后面每一章都是在这个骨架上加东西。

```ts
class Agent {
  constructor(private model: Model) {}

  async run(userInput: string, history: Message[]): Promise<string> {
    // ① 装配上下文
    let messages = [...history, { role: 'user', content: userInput }]
    const tools = this.getTools()   // 当前可用工具（第 4 章）

    while (true) {
      // ② 调 LLM（第 1 章）
      const response = await this.model.chat({ messages, tools })

      // ③ 解析输出
      if (response.text) return response.text   // 文本 → 结束

      // ④⑤ 工具调用 → 执行 → 回填 → 继续循环
      if (response.toolCalls) {
        for (const call of response.toolCalls) {
          const result = await this.executeTool(call)   // 执行（第 4、7 章）
          messages.push({ role: 'assistant', toolCalls: [call] })
          messages.push({ role: 'tool', content: JSON.stringify(result), toolCallId: call.id })
        }
        continue
      }
    }
  }

  getTools() { /* 返回注册的工具 schema */ }
  async executeTool(call: ToolCall) { /* 查表执行 + 权限检查 */ }
}
```

**注意这个骨架里的三个「可扩展点」**，它们正是后续章节的入口：

1. `this.model` —— 抽象出来的模型接口（第 1 章）
2. `this.getTools()` —— 可注册的工具系统（第 4 章）
3. `executeTool` 里的「权限检查」——安全层（第 7 章）

---

## 六、方案设计

### 6.1 验收标准

核心循环「做好」有两条可验证的标准：

1. **收敛**：任何输入都必须在有限轮内结束——要么给出文本答案，要么明确报错，绝不能无限空转。
2. **可观测、可中断**：循环的每一步都能被外部看到（事件流），也能在任意时刻被外部打断（取消）。

第一条是「不会死」，第二条是「能驾驭」。后面所有取舍都服务于这两条。

### 6.2 关键取舍一：事件流 vs 一次性返回值

这是循环对外接口的根本选择。

| | 一次性返回值 | 事件流（AsyncGenerator） |
|---|---|---|
| 实现 | 简单，一个 `Promise<string>` | 稍复杂，要定义事件类型 |
| 体验 | 用户等 30 秒白屏 | 逐字流式输出 |
| UI 复用 | 换 UI 要改核心 | 任何 UI 都是订阅者 |
| 可中断 | 难 | `for await` 里 `break` 即中断 |

**立场：用事件流，即使你现在的唯一前端是 CLI。** 理由：事件流一旦建立，「一个运行时多副面孔」（第 8 章）、「可观测」（第 9 章）、「可取消」全都免费获得。而一次性返回值是条死胡同——将来要流式输出时得推倒重来。

**失效条件**：如果你做的不是交互式 agent，而是纯离线批处理（输入一批数据、输出一批结果、没人盯着看），事件流的收益就很小，此时一次性返回值更简单。但「批处理」其实可以用事件流 + 一个只收集 `text_delta` 的 headless 订阅者实现，所以即便在这个场景，事件流也不亏。

### 6.3 关键取舍二：多工具——串行还是并行

模型一次可能返回多个工具调用，执行顺序有两条路：

- **串行**：按返回顺序逐个执行，每个工具的结果都能立刻影响后续工具。
- **并行**：独立工具并发执行，快，但结果合并和错误处理复杂得多。

**立场：默认串行，并行作为明确的优化再做。** 理由有三：

1. 工具结果常常**互相依赖**（先读文件，再基于内容决定下一步），串行天然安全。
2. 串行的回填顺序清晰：`assistant(toolCalls) → tool(结果) → assistant(toolCalls) → tool(结果)`，模型看到的上下文严格线性。
3. 并行会引入「部分成功、部分失败」的混乱状态，你需要定义「谁等谁、失败怎么合并」，收益却只是省几秒延迟。

**失效条件**：当你有多个**明确无依赖**、且**耗时较长**的工具（如同时查三个互不相干的接口）时，并行才值得。这时建议把「哪些工具可并行」做成工具的声明式属性，而不是在循环里硬编码。

### 6.4 关键取舍三：靠什么保证「收敛」

循环必须有个「一定会停」的机制，三条防线（从弱到强）：

1. **轮数上限**：最多 N 轮 turn，超过就报错退出。最简单，必做。
2. **重复检测**：连续多次用相同参数调同一工具，说明模型陷入死循环，打断。DSH 的 `repeat-tool-reminder` 就是这条。
3. **超时**：单个工具、单次模型调用设超时，卡住就返回「超时」而不是永远等。

**立场：三条都要有，但分优先级落地。** 先上轮数上限（一行代码的事，立刻保证「不会无限空转」），再上超时（防止单步卡死），最后上重复检测（防语义层面的死循环）。不要一开始就做重复检测——它需要记录调用历史、比较参数，复杂度最高，收益却是锦上添花。

### 6.5 落地方案

- **第一步（地基）**：跑通第 0 章那个最小循环——哪怕 `Model` 是假的、工具只有一个。目标是「收敛」这条验收标准先达标。
- **第二步**：把循环改成事件流（`AsyncGenerator<AgentEvent>`），让 CLI 能流式打印。此时「可观测」达标。
- **第三步**：加轮数上限 + 单步超时，保证「可中断、不会死」。
- **第四步（进阶）**：串行基础上，给无依赖工具加并行；再加重复检测。

关键顺序：**先串行跑通，再谈并行；先轮数上限，再谈语义防循环。** 复杂度要按「收益/成本」排序往里加。

### 6.6 反模式（避坑）

1. **❌ 在循环里硬编码超时、重试、防循环逻辑**——结果：策略换一个场景就要改核心。正确做法：把策略抽成钩子（第 3 章的 `LoopHooks`）。
2. **❌ 循环返回一次性字符串**——结果：将来无法流式、无法多 UI、无法取消。正确做法：事件流。
3. **❌ 并行工具无脑全部并发**——结果：有依赖的工具拿到过时数据，产出错误结果。正确做法：默认串行，只对声明为「无依赖」的工具并行。

---

## 七、达成什么目标

读完本章，你应该能：

- ✅ 用自己的话解释「Agent Harness 是什么、为什么需要它」
- ✅ 默写出核心循环的五步
- ✅ 理解 pi 的「事件流循环」和 DSH 的「插件化循环」的区别
- ✅ 写一个最小但能跑的 agent 循环骨架

---

## 八、实操练习

1. **手写循环**：用任意语言（TS/Python 皆可）写出上面那个 `Agent.run()` 的五步循环，先不管 `Model` 和 `Tool` 的真实实现，用假函数 `fakeModel.chat()` 和 `fakeExecuteTool()` 顶替，把循环跑通。
2. **加一个日志**：在循环的每一步 `console.log`，观察「上下文 → 调用 → 解析 → 执行 → 回填」的完整过程。
3. **思考题**：如果模型一次返回了 3 个工具调用，你的循环会怎么走？如果第 2 个工具失败了，第 3 个还执行吗？把你的答案写下来，到第 4 章验证。

---

> 下一章：[第 1 章 模型接入层](01-模型接入层.md) —— 解决「怎么统一调用各家大模型」。
