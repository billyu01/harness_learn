# 第 11 章 从零实现实操

> 上一章：[第 10 章 范式总结与对比](10-范式总结与对比.md) ｜ 下一章：[附录 参考链接](12-参考链接.md)

---

## 一、总览：分阶段实现一个 harness

本章把前面各章的代码骨架，拼成一个**最小但完整**的 Agent Harness。按 6 个 Phase 渐进：

| Phase | 目标 | 对应章节 |
|---|---|---|
| 1 | 最小循环（只有模型 + 工具） | 第 0、3 章 |
| 2 | 工具系统（可注册） | 第 4 章 |
| 3 | 上下文管理（裁剪） | 第 2 章 |
| 4 | 会话持久化 + 分支 | 第 5 章 |
| 5 | 扩展机制 | 第 6 章 |
| 6 | 安全 + 多交互 + 观测 | 第 7、8、9 章 |

**建议**：每个 Phase 都跑通再进下一个，不要一次写完。先跑通 Phase 1 的最小闭环，你就能真正理解「harness 的心脏」是怎么跳的。

---

## 二、Phase 1：最小可行 harness（只有循环）

```ts
// 核心：一个 agent 循环
class Agent {
  constructor(private model: Model) {}

  async run(userInput: string, history: Message[]): Promise<string> {
    // ① 装配上下文
    let messages = [...history, { role: 'user', content: userInput }]
    const tools = this.getTools()

    while (true) {
      // ② 调 LLM
      const response = await this.model.chat({ messages, tools })

      // ③ 文本 → 结束
      if (response.text) return response.text

      // ④⑤ 工具调用 → 执行 → 回填 → 继续
      if (response.toolCalls) {
        for (const call of response.toolCalls) {
          const result = await this.executeTool(call)
          messages.push({ role: 'assistant', toolCalls: [call] })
          messages.push({ role: 'tool', content: JSON.stringify(result), toolCallId: call.id })
        }
        continue
      }
    }
  }

  getTools() { /* 返回工具 schema */ }
  async executeTool(call: ToolCall) { /* 查表执行 + 权限检查 */ }
}
```

**跑通标准**：接一个真实模型（或 FakeModel），配一个 `read` 工具，问「读 a.txt 并总结」，能走完循环、返回答案。

---

## 三、Phase 2：加工具系统（可注册）

```ts
interface Tool {
  name: string
  description: string
  parameters: Record<string, unknown>
  execute: (args: any, env: ToolEnv) => Promise<any>
}

interface ToolEnv { workingDir: string; signal: AbortSignal }

class ToolRegistry {
  private tools = new Map<string, Tool>()
  register(t: Tool) { this.tools.set(t.name, t) }
  unregister(name: string) { this.tools.delete(name) }

  list(): ToolDef[] {
    return [...this.tools.values()].map(({ name, description, parameters }) =>
      ({ name, description, parameters }))
  }

  async run(name: string, args: any, env: ToolEnv) {
    const tool = this.tools.get(name)
    if (!tool) throw new Error(`unknown tool: ${name}`)
    await permissionGate.check(name, args)   // Phase 6 才真正实现
    return tool.execute(args, env)
  }
}
```

---

## 四、Phase 3：加上下文管理（裁剪）

```ts
function transformContext(messages: Message[], maxTokens: number): Message[] {
  // 1. 裁剪：总 token 超限，从最旧开始丢
  // 2. 注入：加入系统提示、当前时间、工作目录等
  return pruned
}

function convertToLlm(messages: AgentMessage[]): LlmMessage[] {
  // 过滤掉 UI 专用消息，只保留 user/assistant/toolResult
}
```

**跑通标准**：构造 100 条消息，`maxTokens` 设很小，验证裁剪后能正常调模型。

---

## 五、Phase 4：加会话持久化 + 分支

```ts
type Entry = { id: string; parentId: string | null; role: string; content: string }

// 每条消息带 id + parentId，存成 JSONL 行，天然支持树形分支
function appendLine(entry: Entry) { fs.appendFile('session.jsonl', JSON.stringify(entry) + '\n') }

function ancestors(id: string, all: Entry[]): Entry[] {
  const byId = new Map(all.map(m => [m.id, m]))
  const path: Entry[] = []
  let cur = byId.get(id)
  while (cur) { path.unshift(cur); cur = cur.parentId ? byId.get(cur.parentId) : undefined }
  return path
}
```

**跑通标准**：跑两轮对话，然后「回到第一轮重新回答」，验证新分支挂对父节点、老分支不变。

---

## 六、Phase 5：加扩展机制

**轻量路线（学 pi）**：

```ts
export default function (agent: ExtensionAPI) {
  agent.registerTool({ name: 'deploy', /* ... */ })
  agent.on('tool_call', async (event) => { /* 拦截 */ })
}
```

**重量路线（学 DSH/Cordis）**：引入 DI 容器，插件声明 `inject`/`provide`，容器负责加载顺序和生命周期回收。

```ts
class Context {
  private services = new Map<string, unknown>()
  private disposers: Array<() => void> = []

  provide<T>(name: string, value: T) { this.services.set(name, value) }
  get<T>(name: string): T { return this.services.get(name) as T }

  effect(setup: () => void | (() => void)) {
    const dispose = setup()
    if (dispose) this.disposers.push(dispose)
  }

  async plugin(p: { inject?: string[]; apply(ctx: Context): void | Promise<void> }) {
    for (const dep of p.inject ?? []) if (!this.services.has(dep)) return
    await p.apply(this)
  }

  async dispose() { for (const d of this.disposers) d(); this.disposers = [] }
}
```

---

## 七、Phase 6：加安全 + 多交互形态 + 观测

### 安全（第 7 章）

```ts
class PermissionGate {
  constructor(private rules: PermissionRule[], private ask: (q: string) => Promise<boolean>) {}
  async check(tool: string, args: any) {
    for (const rule of this.rules) {
      if (!rule.match(tool, args)) continue
      const d = rule.decision(tool, args)
      if (d === 'deny') throw new Error(`blocked: ${tool}`)
      if (d === 'ask') { if (!await this.ask(`${tool} ${JSON.stringify(args)}?`)) throw new Error(`declined: ${tool}`) }
    }
  }
}
```

### 多交互形态（第 8 章）

```ts
// CLI：边收边打印
for await (const ev of agent.run(input, history)) {
  if (ev.type === 'text_delta') process.stdout.write(ev.delta)
}

// headless：只要最终答案
let text = ''
for await (const ev of agent.run(input, history)) if (ev.type === 'text_delta') text += ev.delta
return text
```

### 观测（第 9 章）

```ts
class Tracer {
  private spans: Span[] = []
  trace<T>(type: string, fn: () => Promise<T>, meta = {}): Promise<T> {
    const start = Date.now()
    return fn().then(r => { this.spans.push({ type, start, end: Date.now(), meta }); return r })
  }
}
```

---

## 八、最终组装

```ts
const models = new Models()
models.register(new OpenAICompatibleModel('deepseek', 'deepseek-chat', BASE, () => key))

const tools = new ToolRegistry()
tools.register(readTool); tools.register(writeTool); tools.register(bashTool)

const ctx = new DefaultContextManager(128_000, countTokens)
const store = new JsonlSessionStore('./session.jsonl')
const gate = new PermissionGate([...], askUser)
const tracer = new Tracer()

const agent = new Agent(models.get('deepseek','deepseek-chat')!, tools, ctx, {
  beforeTool: (call) => gate.check(call.name, call.arguments),
})

// 各模块的配合：
//   ContextManager 决定「发给模型什么」→ Model 决定「怎么调」→ 循环决定「何时调」
//   ToolRegistry + PermissionGate 决定「能做什么、做了什么」
//   SessionStore 决定「记住什么、能否回退」
//   ExtensionHost 决定「还能加什么」
//   Emitter + Tracer 决定「怎么看它、怎么观测它」
```

---

## 九、达成什么目标

读完本章，你应该能：

- ✅ 按 Phase 1–6 分阶段实现一个能跑的 harness
- ✅ 理解「先跑通循环，再逐层加能力」的迭代顺序
- ✅ 把九个模块的代码骨架拼成一个完整项目

---

## 十、实操练习（终极挑战）

1. **完整实现**：把 Phase 1–6 的代码整理成一个 TS 项目（`package.json` + 分模块 `src/`），接真实 DeepSeek API，跑通「读文件 → 总结 → 写文件」的完整任务。
2. **加降级路由**：在模型层加「A 模型失败切 B 模型」的降级逻辑（第 1 章）。
3. **加一个扩展**：写一个 `deploy` 扩展，注册成工具，让 agent 能「部署当前项目」。

---

> 下一章：[附录 参考链接](12-参考链接.md) —— 仓库、文档、延伸阅读。
