# 第 11 章 从零实现实操

> 上一章：[第 10 章 范式总结与对比](10-范式总结与对比.md) ｜ 下一章：[附录 参考链接](12-参考链接.md)

---

## 一、总览：分阶段实现一个 harness

本章是第 1–9 章的**复盘与组装**。每个模块的代码骨架已经在对应章节给过，这里不再重复贴代码，只做三件事：

1. 给出**分阶段路线图**（先做什么、后做什么）；
2. 每个 Phase 的**跑通标准**（怎么算做完了）；
3. 最后把所有模块**组装**成一个能跑的整体。

分 6 个 Phase 渐进：

| Phase | 目标 | 对应章节 |
|---|---|---|
| 1 | 最小循环（只有模型 + 工具） | 第 3 章 |
| 2 | 工具系统（可注册） | 第 4 章 |
| 3 | 上下文管理（裁剪） | 第 2 章 |
| 4 | 会话持久化 + 分支 | 第 5 章 |
| 5 | 扩展机制 | 第 6 章 |
| 6 | 安全 + 多交互 + 观测 | 第 7、8、9 章 |

**核心顺序**：先跑通「循环」（Phase 1）这个心脏，再逐层加能力。每个 Phase 都跑通再进下一个，不要一次写完。

---

## 二、Phase 1：最小循环（先跑通心脏）

第 3 章给出了完整的事件流循环。这里强调「最小可行」版本的最小集合：

- 一个 `Model`（可以先写 `FakeModel`，不调网络，第 1 章有）
- 一个工具（先只做 `read`）
- 循环骨架：`while` + 「调模型 → 有工具调用就执行回填 → 再调」

> 第 3 章的 `Agent.run()` 是**完整版**（事件流 + 轮数上限 + 工具回填 + 钩子）。刚开始可以只抄它的骨架，略过事件流的类型定义，用 `for await` 打印 `text_delta` 即可。

**跑通标准**：接一个 `FakeModel`（或真实 DeepSeek）+ 一个 `read` 工具，问「读 a.txt 并总结」，能走完循环、返回答案。

---

## 三、Phase 2–6：要点速览（详见各章）

这五个 Phase 的代码骨架都在对应章节，这里只列「这一步要补什么」和「跑通标准」，不重复贴代码。

### Phase 2：工具系统（第 4 章）

**要补**：`Tool` 四要素 + `ToolRegistry`（`register / list / run`），`list()` 只给 schema、`run()` 才执行，并在 `run()` 里加参数校验和结果截断。

**跑通标准**：能注册多个工具，`list()` 喂给模型后模型能正确调用；参数错误时返回结构化错误而非 crash。

### Phase 3：上下文管理（第 2 章）

**要补**：`ContextManager.prepare()`——两层消息转换（`transformContext` + `convertToLlm`）+ 按优先级裁剪（工具结果 > 助手 > 用户 > 系统提示）。

**跑通标准**：构造 100 条消息、`maxTokens` 设很小，验证裁剪后能正常调模型，且系统提示不被裁掉。

### Phase 4：会话持久化 + 分支（第 5 章）

**要补**：`Entry { id, parentId }` + JSONL append-only 存储 + `ancestors(id)` / `children(id)`。

**跑通标准**：跑两轮对话后「回到第一轮重新回答」，验证新分支挂对父节点、老分支不变。

### Phase 5：扩展机制（第 6 章）

**要补**：轻量 `ExtensionHost`（`registerTool` / `registerCommand` / `on()` 返回 disposer）。进阶再上重量 `Context`（DI + effect 回收）。

**跑通标准**：写一个扩展注册 `deploy` 工具，能被循环调用；卸载扩展后副作用（定时器/监听器）被清理。

### Phase 6：安全 + 多交互 + 观测（第 7、8、9 章）

**要补**：`PermissionGate`（deny/ask/allow）接进 `run()`；headless/RPC 订阅者；`Tracer` 在「模型调用」「工具执行」两处埋点。

**跑通标准**：越权操作被 deny、危险操作触发 ask；headless 能输出最终答案；一次任务结束后能导出 spans 做成本归因。

---

## 四、最终组装

把九个模块拼成一个整体（代码骨架见各章，这里只展示接线关系）：

```ts
const models = new Models()                                    // 第 1 章
models.register(new OpenAICompatibleModel('deepseek', 'deepseek-chat', BASE, () => key))

const tools = new ToolRegistry()                               // 第 4 章
tools.register(readTool); tools.register(writeTool); tools.register(bashTool)

const ctx = new DefaultContextManager(128_000, countTokens)    // 第 2 章
const store = new JsonlSessionStore('./session.jsonl')         // 第 5 章
const gate = new PermissionGate([...], askUser)                // 第 7 章
const tracer = new Tracer()                                    // 第 9 章

const agent = new Agent(models.get('deepseek', 'deepseek-chat')!, tools, ctx, {
  beforeTool: (call) => gate.check(call.name, call.arguments), // 第 7 章
})

// 各模块的配合：
//   ContextManager 决定「发给模型什么」→ Model 决定「怎么调」→ 循环决定「何时调」
//   ToolRegistry + PermissionGate 决定「能做什么、做了什么」
//   SessionStore 决定「记住什么、能否回退」
//   ExtensionHost（第 6 章）决定「还能加什么」
//   Emitter（第 8 章）+ Tracer 决定「怎么看它、怎么观测它」
```

> **两点接线提醒**：① 上面的 `store`（会话存储）和 `Emitter`（事件总线）是「待接线」的——`store` 要接进循环的「每条消息落盘」，`Emitter` 要接进循环的「边 yield 边 emit」；这两处接线是第 5、8 章的核心，组装时别漏。② `Agent` 构造函数签名以第 3 章为准：`(model, tools, ctx, hooks)`。

---

## 五、达成什么目标

读完本章，你应该能：

- ✅ 按 Phase 1–6 分阶段实现一个能跑的 harness
- ✅ 理解「先跑通循环，再逐层加能力」的迭代顺序
- ✅ 把九个模块的代码骨架拼成一个完整项目，且接线不遗漏

---

## 六、实操练习（终极挑战）

1. **完整实现**：把各章代码整理成一个 TS 项目（`package.json` + 分模块 `src/`），接真实 DeepSeek API，跑通「读文件 → 总结 → 写文件」的完整任务。
2. **加降级路由**：在模型层加「A 模型失败切 B 模型」的降级逻辑（第 1 章）。
3. **加一个扩展**：写一个 `deploy` 扩展，注册成工具，让 agent 能「部署当前项目」（第 6 章）。
4. **补全接线**：把 `store` 落盘、`Emitter` 广播这两处接线补上，让会话真正可恢复、事件真正可被多监听者消费。

---

> 下一章：[附录 参考链接](12-参考链接.md) —— 仓库、文档、延伸阅读。
