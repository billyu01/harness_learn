# 第 8 章 交互形态（Interaction Modes）

> 上一章：[第 7 章 安全与隔离](07-安全与隔离.md) ｜ 下一章：[第 9 章 观测与遥测](09-观测与遥测.md)

---

## 一、概念：同一个 agent，多种用法

同一个 agent，用户可能想：

- 在**终端**里交互式地用（打字、看流式输出）
- 在**浏览器**里用（Web GUI）
- 在 **CI/脚本**里无头跑（没人盯着，只要最终结果）
- 被**别的程序**调用（作为 SDK 或 RPC 服务）

**交互形态要解决的问题**：核心运行时只写一次，如何驱动多种完全不同的前端？

---

## 二、原理：事件流是「运行时与 UI 的分界线」

答案藏在第 3 章的事件流里。核心运行时（`Agent.run()`）**只产出事件**，不碰任何具体 UI。CLI、Web、headless 都是**事件的订阅者**：

```
核心运行时（Agent.run） —— 产出 AgentEvent 流
        │
        ├── 订阅者 1：CLI —— 把 text_delta 写到终端
        ├── 订阅者 2：Web —— 把事件推给浏览器（WebSocket）
        ├── 订阅者 3：headless —— 只收集 text，最后打印
        └── 订阅者 4：SDK/RPC —— 序列化事件发给外部进程
```

**这就是「一个运行时，多副面孔」的本质**：运行时与 UI 完全解耦，UI 只是消费者。

---

## 三、pi 的实现：四种模式 + SDK

pi 提供四种运行模式，外加 SDK 嵌入：

| 模式 | 用途 |
|---|---|
| 交互式（默认） | 终端 TUI，打字对话 |
| print | 非交互，直接输出 |
| JSON（`--mode json`） | 结构化输出，适合自动化 |
| RPC（`--mode rpc`） | stdin/stdout JSONL 协议，非 Node 集成 |

外加 **SDK**（`createAgentSession`）嵌入到自己的应用。

终端 UI 用 `pi-tui`（差分渲染的终端 UI 库）实现。pi 的架构里，**事件流是核心，TUI 只是订阅者**。

---

## 四、DSH 的实现：profile 决定 UI，UI 是插件

DSH 的交互形态由 **profile** 决定：

- `dsh web`（`--profile web`）：启动 Web GUI。
- `dsh --profile headless "任务"`：无头跑一个任务，打印最终答案退出。

CLI 启动器 `dsh` 只管「**选 profile**」，真正的 UI 是 profile 里的插件（`dsh-web-app`、`dsh-headless`）。

**DSH 的启示**：UI 不是核心的一部分，而是「挂在 profile 上的插件」。想加一种新交互形态？写一个新 profile（或新插件），核心运行时不用动。

---

## 五、我们如何设计：事件发射器 + 多种订阅者

```ts
class Emitter {
  private handlers = new Map<string, Set<(p: any) => void>>()
  on(ev: string, h: (p: any) => void) { (this.handlers.get(ev) ?? new Set()).add(h) }
  off(ev: string, h: (p: any) => void) { this.handlers.get(ev)?.delete(h) }
  emit(ev: string, p: any) { this.handlers.get(ev)?.forEach(h => h(p)) }
}
```

> **`Emitter` 和 `Agent.run()` 事件流是两回事，别混淆。** `Agent.run()` 是**拉取式**的 `AsyncGenerator`——前端 `for await` 逐条消费，一个订阅者消费了不影响别人；`Emitter` 是**推送式**的广播总线——一个事件广播给所有监听者。它们的用途不同：前端消费运行时用 `AsyncGenerator`（下面三个 `runXXX`），而扩展要「拦截事件、挂多个监听」时用 `Emitter`（第 6 章的 `on()`）。二者可以共存：循环内部边 `yield` 边 `emitter.emit`，即可同时服务「单消费者」和「多监听者」。

```ts
// 三种前端都是这么消费同一个 Agent.run() 的事件流：

// CLI：边收边打印
async function runCLI(agent: Agent, input: string) {
  for await (const ev of agent.run(input, history)) {
    if (ev.type === 'text_delta') process.stdout.write(ev.delta)
    if (ev.type === 'toolcall_start') console.log('\n[执行工具]', ev.call.name)
  }
}

// headless：只要最终答案
async function runHeadless(agent: Agent, input: string): Promise<string> {
  let text = ''
  for await (const ev of agent.run(input, history)) if (ev.type === 'text_delta') text += ev.delta
  return text
}

// Web：推给浏览器
async function runWeb(agent: Agent, input: string, ws: WebSocket) {
  for await (const ev of agent.run(input, history)) ws.send(JSON.stringify(ev))
}

// RPC：序列化给外部进程
async function runRPC(agent: Agent, input: string) {
  for await (const ev of agent.run(input, history)) {
    process.stdout.write(JSON.stringify(ev) + '\n')   // JSONL 协议
  }
}
```

**几个关键设计决策**：

1. **事件流用 `AsyncGenerator`**：`for await` 天然消费，天然可中断（`break`）。
2. **headless 只关心 `text_delta`**：其他事件（工具调用等）可以选择性忽略——这就是「无头模式」的简单实现。
3. **RPC 用 JSONL**：每个事件序列化成一行 JSON，任何语言都能解析（呼应 pi 的 `--mode rpc`）。

---

## 六、方案设计

### 6.1 验收标准

交互形态「做好」有两条可验证的标准：

1. **一份运行时，多种前端**：核心循环只写一次，CLI / Web / headless / RPC 都是它的订阅者，加一个新前端不改核心。
2. **双向可控**：不仅运行时→UI 的事件能推出去，UI→运行时的指令（用户输入、停止、审批）也能传回来。

第一条是「解耦」，第二条是「闭环」——只推不收的交互是残缺的。

### 6.2 关键取舍一：事件协议的边界——进程内还是跨进程

事件流要在哪传播，决定了它的形态：

- **进程内**：事件是内存对象，任意复杂类型都行。
- **跨进程**（Web/RPC）：事件必须可序列化（JSON）。

**立场：从一开始就把事件设计成「可序列化」，即使你现在只在进程内用。** 理由：一旦事件里混进函数、类实例等不可序列化的东西，将来做 Web/RPC 时就得重构。而「可序列化」几乎不损失表达力——所有 agent 事件本质都是「文本 + 结构化数据」，用 JSON 完全够。

**失效条件**：极少数需要传递「活对象」（如流式文件句柄）的场景，这种走单独的「带外通道」，不进事件流。

### 6.3 关键取舍二：取消信号怎么反向传导

用户在前端点「停止」，这个取消要穿越「前端 → 运行时 → 正在执行的工具」，是交互形态最容易做砸的地方。

**立场：用 `AbortSignal` 一路传到底，而不是「前端发个消息、运行时看心情停」。** 理由：`AbortSignal` 是标准的、可组合的取消机制，工具层（`ToolEnv.signal`）和模型层（`AbortSignal`）都能直接消费。它在第 3 章循环、第 4 章工具里都预留了，这里把它「从 UI 穿到底」。

**失效条件**：跨进程时（Web/RPC），信号不能直接传对象，需要「UI 发 cancel 事件 → 运行时触发本地 AbortSignal」的两段式。但「运行时内部」这一端仍统一用 AbortSignal。

### 6.4 关键取舍三：headless 的定位

headless 模式（无 UI，只要最终结果）常被当成「阉割版交互」，其实它有独立价值。

**立场：headless 是「一等公民」，不是「没写 UI 的凑合」。** 理由：headless 是天然的**集成测试入口**（CI 里跑）、**批处理入口**（批量任务）、**RPC 的基础**（RPC 就是 headless + 事件序列化）。把它当成正经模式设计，你会免费获得测试和自动化能力。

**失效条件**：无。任何 harness 都应该有 headless，哪怕只是「收集 text_delta 输出最终结果」的十行订阅者。

### 6.5 落地方案

- **第一步（地基）**：事件流 + CLI 订阅者（边收边打印）。先跑通「一份运行时，一个前端」。
- **第二步**：headless 订阅者（只收集最终文本）。此时「多前端」初步达标，且免费获得 CI 集成能力。
- **第三步（闭环）**：加取消信号的反向传导（UI → AbortSignal → 工具/模型）。
- **第四步（进阶）**：RPC（JSONL 序列化）/ Web（WebSocket 推送 + 断线重连）。

关键顺序：**先「能推」（事件流出去），再「能收」（取消传回来），最后「能跨进程」（序列化）。** 不要一上来就做 Web 断线重连——那是最后一步的优化。

### 6.6 反模式（避坑）

1. **❌ 事件里塞不可序列化的对象**——结果：将来做 Web/RPC 时推倒重来。正确做法：事件从一开始就 JSON 友好。
2. **❌ 取消靠「发消息」而不是 AbortSignal**——结果：工具收不到、停不下来、卡死。正确做法：AbortSignal 一路传到底。
3. **❌ 把 headless 当成「临时脚本」随便写**——结果：失去测试和自动化的免费入口。正确做法：headless 是一等公民。
4. **❌ 前端直接调用运行时内部方法**——结果：UI 和运行时耦合，换前端要改核心。正确做法：前端只订阅/发送事件，不碰内部。

---

## 七、达成什么目标

读完本章，你应该能：

- ✅ 解释「事件流是运行时与 UI 的分界线」
- ✅ 说明 pi 的四种模式和 DSH 的 profile 化 UI
- ✅ 用同一个 `Agent.run()` 写出 CLI、headless、Web、RPC 四种前端
- ✅ 复述「事件可序列化」「AbortSignal 反向取消」「headless 一等公民」三个关键取舍

---

## 八、实操练习

1. **写 headless 模式**：给第 3 章的循环套一个 `runHeadless`，CI 里跑「生成 PR 摘要」，只输出最终文本。
2. **写 RPC 模式**：把事件流序列化成 JSONL 到 stdout，用另一个进程（任何语言）逐行解析。
3. **思考题**：Web 场景下，用户点「停止」按钮，你如何把这个「取消」信号从浏览器传回核心运行时，中断正在执行的工具？（提示：关联第 4 章的 `ToolEnv.signal`）

---

> 下一章：[第 9 章 观测与遥测](09-观测与遥测.md) —— 不观测，就无法优化；轨迹是诊断 agent 的核心数据。
