# 第 1 章 模型接入层（Model Provider）

> 上一章：[首页](Agent-Harness设计范式教程.md) ｜ 下一章：[第 2 章 消息与上下文管理](02-消息与上下文管理.md)

---

## 一、概念：为什么需要「模型接入层」

市面上有 OpenAI、Anthropic、Google、DeepSeek、Mistral，还有一堆 OpenAI 兼容的私有网关（vLLM、企业内部模型）。它们的问题在于：

- **请求格式不同**：字段名、嵌套结构各异。
- **响应格式不同**：有的叫 `choices[0].message`，有的叫 `content`，流式协议（SSE）细节也不同。
- **鉴权不同**：有的用 `Authorization: Bearer`，有的用 `x-api-key`，有的是 OAuth。
- **工具调用协议不同**：OpenAI 用 `tools` + `tool_calls`，Anthropic 用 `tools` + `tool_use`，格式和字段都不一样。

**模型接入层（Model Provider）的目标**：把这些差异全部吸收掉，向上层暴露一个**统一接口**。这样，核心循环（第 3 章）只依赖这一个接口，永远不用关心「背后是哪家模型」。

---

## 二、原理：适配器模式 + 注册表

模型接入层由两个经典设计模式组成：

1. **适配器模式（Adapter）**：为每一家厂商写一个 adapter，把它的私有协议翻译成统一接口。上层只面向统一接口编程。
2. **注册表（Registry）**：用一个 `Models` 集合，按 `vendor/name` 寻址，屏蔽「模型名字」这个厂商强相关的概念。

> 下面代码里的 `LlmMessage`、`ToolDef`、`ToolCall` 分别是「喂给模型的消息」「工具 schema」「工具调用」，第一次出现先按名字理解，正式定义见第 2 章（`LlmMessage`）和第 4 章（`ToolDef`/`ToolCall`）。

```ts
// 统一接口：上层只认这个
interface Model {
  vendor: string
  name: string
  stream(input: { messages: LlmMessage[]; tools: ToolDef[] }): AsyncIterable<ModelEvent>
}

// 统一事件：所有厂商的流式输出都翻译成这套
type ModelEvent =
  | { type: 'text_delta'; delta: string }
  | { type: 'toolcall_delta'; call: ToolCall }
  | { type: 'done'; reason: string }
  | { type: 'error'; error: Error }

// 注册表：按 "vendor/name" 寻址
class Models {
  private registry = new Map<string, Model>()
  register(m: Model) { this.registry.set(`${m.vendor}/${m.name}`, m) }
  get(vendor: string, name: string) { return this.registry.get(`${vendor}/${name}`) }
}
```

**为什么返回 `AsyncIterable<ModelEvent>` 而不是一次性返回完整结果？** 因为流式。模型逐 token 生成，harness 要边生成边转发给用户（第 3 章讲的事件流）。统一事件流，就是「所有厂商差异的终点」。

---

## 三、pi 的实现：`pi-ai` 包

pi 把模型接入层单独做成了 `pi-ai` 包（这是 pi 里**被 DSH 复用的部分**）。它的设计要点：

- **`Models` 集合 + `getModel(vendor, name)`**：统一寻址，屏蔽厂商差异。
- **`builtinModels()`**：一行注册所有内置 provider；关心包体积的应用则按需单独注册（tree-shaking）。
- **自动鉴权解析**：按约定从环境变量 / credential store 解析 API Key（如 `OPENAI_API_KEY`、`ANTHROPIC_API_KEY`），不在启动时硬编码。
- **token / 费用追踪**：每次调用统一记录用量和成本。
- **跨提供商切换（hand-off）**：会话中把上下文交给另一个模型继续。
- **只收录支持 function calling 的模型**：因为工具调用是 agentic 工作流的刚需。

```ts
import { builtinModels } from '@earendil-works/pi-ai/providers/all'
const models = builtinModels()
const model = models.getModel('openai', 'gpt-4o-mini')!

const stream = models.stream(model, { messages, tools })
for await (const event of stream) {
  if (event.type === 'text_delta') process.stdout.write(event.delta)
}
```

---

## 四、DSH 的实现：把模型层包成「可替换的插件」

DSH 复用 pi 的 `pi-ai`（`dsh-llm-pi-ai` 包），但**把它包成了一个 Cordis 服务（插件）**，并额外提供了多个模型层插件：

- `dsh-llm-deepseek`：DeepSeek 官方适配器。
- `dsh-llm-pi-ai`：pi-ai 适配器（官方描述为「design-verification twin」，即设计验证的双胞胎）。
- `dsh-llm-retry`：模型调用失败自动重试。

**这体现了 DSH 的核心哲学**：在 DSH 里，「模型接入」本身也是一个**可替换的插件**。你想换一家模型供应商，不是改代码，而是换一个插件（或改配置）。

---

## 五、我们如何设计：一个 OpenAI 兼容 adapter

大多数私有模型（DeepSeek、vLLM、内部网关）都兼容 OpenAI 协议，所以「一个 OpenAI 兼容 adapter」能覆盖 90% 的场景。

```ts
class OpenAICompatibleModel implements Model {
  constructor(
    public vendor: string,
    public name: string,
    private baseURL: string,
    private apiKey: () => string,   // 懒解析：调用时才取
  ) {}

  async *stream({ messages, tools }: { messages: LlmMessage[]; tools: ToolDef[] }) {
    const res = await fetch(`${this.baseURL}/chat/completions`, {
      method: 'POST',
      headers: { Authorization: `Bearer ${this.apiKey()}` },
      body: JSON.stringify({ model: this.name, messages, tools, stream: true }),
    })

    if (!res.ok) {
      yield { type: 'error', error: new Error(`HTTP ${res.status}: ${await res.text()}`) }
      return
    }

    // 逐行解析 SSE，把厂商的 delta 翻译成统一事件
    for await (const chunk of readSSE(res)) {
      const delta = chunk.choices?.[0]?.delta
      if (delta?.content) yield { type: 'text_delta', delta: delta.content }
      if (delta?.tool_calls) yield { type: 'toolcall_delta', call: parseToolCall(delta.tool_calls) }
    }
    yield { type: 'done', reason: 'stop' }
  }
}
```

**几个关键设计决策**：

1. **`apiKey: () => string` 用函数而不是字符串**：懒解析，key 在调用时才从环境变量/配置读取，支持运行时切换。
2. **`stream: true` + SSE 逐行解析**：所有 OpenAI 兼容接口都支持，通用性强。
3. **错误也作为一个事件 `yield`**：不 throw，让上层循环统一处理错误（第 3 章会讲）。

---

## 六、方案设计

### 6.1 验收标准

模型层「做好」有两条可验证的标准：

1. **可替换**：换一家模型供应商，上层（循环、工具、会话）一行代码不改。
2. **不可见**：上层完全不知道「背后是哪家、鉴权怎么做的、流式协议长什么样」。

这两条是「统一接口」的具体化。下面所有取舍都服务于它们。

### 6.2 关键取舍一：统一成流式，还是流式/非流式并存

| | 并存 | 统一成流式 |
|---|---|---|
| 接口数量 | 两套（`chat` + `stream`） | 一套（`stream`） |
| 上层负担 | 要判断用哪个 | 非流式只是「流式 + 收集到最后」 |
| 适配器负担 | 每家在两个接口里重复解析 | 只需实现流式 |

**立场：只保留流式接口，非流式是流式的特例。** 理由：任何非流式调用都能用「跑完流、取最终结果」实现，反之不行。保留两套接口的唯一后果，是让每家适配器都多写一份几乎重复的解析逻辑。

**失效条件**：基本没有。唯一例外是某些 provider 只提供非流式 API（罕见），这时在适配器内部「把非流式结果切块成流式」即可，对外仍只暴露流式接口。

### 6.3 关键取舍二：重试放在哪一层

模型调用报错（尤其是限流 429、网络超时），重试逻辑有两条路：

- **放在适配器内**：每家适配器自己处理重试。
- **放在上层（循环或独立重试层）**：统一处理，适配器只负责「单次调用 + 报错」。

**立场：放在上层，适配器只做「一次调用 + 把错误如实上报」。** 理由有二：

1. 重试策略（重试几次、退避多久、哪些错误可重试）是**跨供应商一致的**，放上层写一遍就行，放适配器里要每家写一遍。
2. 适配器「如实上报错误」让上层能**感知**失败，从而做降级、切模型、通知用户。如果适配器自己把错误吞掉重试，上层就瞎了。

这正是 DSH 把 `dsh-llm-retry` 做成**独立插件**（而非塞进每个模型适配器）的原因。

**失效条件**：某些 provider 的限流重试需要读它返回的特定 header（如 `Retry-After`），这种「供应商特有」的细节适合在适配器内做。但「重试几次、总体退避策略」仍应留在上层。

### 6.4 关键取舍三：模型发现——静态目录 vs 动态拉取

模型列表从哪来：

- **静态目录**：把已知模型硬编码/打包进去（pi 支持静态目录读取）。
- **动态拉取**：运行时从 provider 拉取最新模型列表。

**立场：以静态目录为默认，动态拉取作为可选增强。** 理由：静态目录**确定性高、零网络依赖、可离线**，对「调用已知模型」这个核心场景完全够用。动态拉取的价值在于「发现新模型、跟随 provider 更新」，但它引入网络依赖和失败点，且模型列表变动并不频繁。

**失效条件**：做「模型市场」或需要展示 provider 全部最新模型时，才值得动态拉取。即便如此，也建议「静态兜底 + 动态刷新」，而不是只依赖动态。

### 6.5 落地方案

- **第一步（地基）**：定义统一 `Model` 接口 + `ModelEvent` 事件，写一个 OpenAI 兼容适配器（覆盖 DeepSeek/vLLM/私有网关 90% 场景）。此时「可替换」初步达标。
- **第二步**：写 `FakeModel`（不调网络的假模型），让上层循环和工具能在**不花钱**的前提下开发测试。
- **第三步**：加独立重试层（429 退避、超时重试），放在适配器之上。
- **第四步（进阶）**：加多模型路由/降级（A 失败切 B，第 11 章）、静态模型目录。

关键顺序：**先让「一种模型能跑通」，再谈「多种模型切换」**。统一接口 + 一个真适配器 + 一个假适配器，是地基中的地基。

### 6.6 反模式（避坑）

1. **❌ 鉴权信息写死在适配器代码里**——结果：密钥泄露、无法切换。正确做法：`apiKey: () => string` 懒解析，运行时从环境/配置取。
2. **❌ 适配器内部吞掉错误并无限重试**——结果：上层永远不知道「模型其实一直失败」，降级和通知全部失效。正确做法：如实上报错误，重试策略交给上层。
3. **❌ 为每家供应商各写一套调用方式，散落在业务代码里**——结果：换模型要改核心循环。正确做法：所有厂商差异收进适配器，上层只认统一接口。
4. **❌ 把「计费的 token 精算」和「调用的 token 处理」耦合**——结果：为了计费精确引入 tokenizer，污染了调用路径。正确做法：计费归计费（第 9 章），调用归调用。

---

## 七、达成什么目标

读完本章，你应该能：

- ✅ 解释「适配器模式 + 注册表」如何屏蔽厂商差异
- ✅ 说明 pi-ai 被 DSH 复用的方式，以及 DSH 如何把模型层插件化
- ✅ 自己写一个 OpenAI 兼容的 adapter，接入 DeepSeek 或私有 vLLM
- ✅ 复述「统一流式」「重试放上层」「静态目录优先」三个关键取舍

---

## 八、实操练习

1. **接 DeepSeek**：用上面的 `OpenAICompatibleModel`，接 `https://api.deepseek.com` + `deepseek-chat`，跑通一次「问它一句话」的流式输出。
2. **写 FakeModel**：写一个不调网络的假模型，`stream()` 直接 `yield` 一段固定文本，用来测你的核心循环（第 3 章的循环骨架）。
3. **思考题**：如果模型返回 429（限流），你的 adapter 应该 `yield error` 还是自动重试？自动重试放在 adapter 里好，还是放在上层循环里好？为什么？

---

> 下一章：[第 2 章 消息与上下文管理](02-消息与上下文管理.md) —— 解决「上下文窗口有限，怎么取舍」。
