# Agent Harness 设计范式：从 pi 与 DSH 学架构

> 一本书，教会你「编码智能体框架」的概念、原理、设计理念与实操。
> 以 **pi**（Pi Agent Harness）和 **DSH**（DeepSeek Harness）两个真实源码为样本，
> 提炼出可复用的 **Agent Harness 设计范式**，让你能按需自行实现。

---

## 阅读指南

这不是一份 API 文档，而是一本「讲原理、讲设计」的书。每一章都遵循同一个叙事结构，帮你从「会用」走到「会设计」：

> **概念**（它是什么，解决什么问题）→ **原理**（为什么这样设计）→ **pi 怎么实现** → **DSH 怎么实现** → **我们怎么设计与实现**（含代码骨架）→ **方案设计**（验收标准 → 关键取舍 → 落地方案 → 反模式）→ **达成什么目标** → **实操练习**

其中「方案设计」是每章的重心：它不再罗列「要考虑哪些维度」，而是带着读者做决策——先立可验证的验收标准，再只挑 2～3 个真正难的关键取舍讲透（给出明确立场、失效条件），然后把决策串成可执行的落地路径，最后补反模式避坑。

建议按顺序读：先读首页的心智模型，再逐个击破九大模块（第 1–9 章），最后回到整体做对比与实操（第 10–12 章）。

---

## 先建立心智模型：Agent Harness 到底是什么

"Harness" 本意是**挽具**（套在牲畜身上让它干活的装置）。Agent Harness 就是套在 LLM 身上的那层软件：**把一个大模型，包装成一个能自主完成任务的智能体**。

裸 LLM 只会「接一句话、回一句话」，它不会：

- 自己读文件、跑命令
- 记住多轮上下文、在超长对话里做取舍
- 调用外部工具并处理结果
- 持久化会话、支持分支
- 让用户扩展它的能力

Harness 就是补齐这些的那层胶水。**它的核心，是一个循环**：

```
用户输入
   │
   ▼
① 上下文装配（把历史消息 + 工具定义 + 系统提示组装成一次 LLM 请求）
   │
   ▼
② 调用 LLM（流式）
   │
   ▼
③ 解析输出 ── 是文本？── 是 → 输出给用户，结束
   │
   是工具调用？
   │
   ▼
④ 执行工具（在沙箱里，过权限检查）
   │
   ▼
⑤ 把工具结果回填到上下文 → 回到 ② 继续循环
```

**pi 和 DSH 都是这个循环的不同实现**。你理解了这一步，就理解了后面所有模块存在的意义——每个模块都是为了「把这个循环做得更稳、更安全、更可扩展」。

---

## 目录

| 章节 | 主题 | 一句话 |
|---|---|---|
| [第 1 章](01-模型接入层.md) | 模型接入层 | 统一接口，屏蔽厂商差异 |
| [第 2 章](02-消息与上下文管理.md) | 消息与上下文管理 | 窗口有限，取舍有术 |
| [第 3 章](03-Agent循环.md) | Agent 循环 | 状态机与策略钩子 |
| [第 4 章](04-工具系统.md) | 工具系统 | schema 决定「怎么调」，执行决定「做什么」 |
| [第 5 章](05-会话与分支.md) | 会话与分支 | 树形历史，可回溯可探索 |
| [第 6 章](06-扩展机制.md) | 扩展机制 | 框架与脚本的分水岭（灵魂） |
| [第 7 章](07-安全与隔离.md) | 安全与隔离 | 协作式信任 vs 强边界 |
| [第 8 章](08-交互形态.md) | 交互形态 | 一个运行时，多副面孔 |
| [第 9 章](09-观测与遥测.md) | 观测与遥测 | 不观测就无法优化 |
| [第 10 章](10-范式总结与对比.md) | 范式总结与对比 | pi 与 DSH：两种哲学 |
| [第 11 章](11-从零实现实操.md) | 从零实现实操 | Phase 1–6 落地骨架 |
| [附录](12-参考链接.md) | 参考链接 | 仓库、文档、延伸阅读 |

---

## 一句话总结范式

> **Agent Harness = 统一模型层 + 事件流运行时 + 可插拔工具 + 树形会话 + 扩展机制 + 隔离 + 观测。**
>
> 其中**核心循环**是心脏，**扩展机制**是灵魂，**事件流**是让「一个运行时多副面孔」的关键，**分支会话**是区别于聊天机器人的分水岭。

pi 教你「如何做得极简而可扩展」，DSH 教你「如何做成可承载大型系统的插件平台」。两者合起来，就是一套完整的 agent harness 设计方法论。

**现在，从 [第 1 章](01-模型接入层.md) 开始读起。**
