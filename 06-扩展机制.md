# 第 6 章 扩展机制（Extension Mechanism）

> 上一章：[第 5 章 会话与分支](05-会话与分支.md) ｜ 下一章：[第 7 章 安全与隔离](07-安全与隔离.md)

---

## 一、概念：框架与脚本的分水岭

一个 agent 如果只能干「写死的几件事」，那它是个**脚本**。一个 agent 如果能被用户、第三方「加能力」，那它是个**框架**。

**扩展机制就是这道分水岭。** 它回答的核心问题：**用户/第三方怎么给智能体加能力？**

- 加一个工具？加一个命令？加一个 UI 组件？
- 拦截一个事件？替换一个内置行为？
- 打包分享给别人？

---

## 二、原理：两条路线

业界有两种扩展哲学的典型实现：

| | 轻量路线（pi） | 重量路线（DSH/Cordis） |
|---|---|---|
| 形态 | TS 函数（`export default (api) => {}`） | 插件（声明 `inject`/`provide`） |
| 门槛 | 低，一个文件即可 | 高，要理解 DI 容器 |
| 依赖关系 | 扁平，无显式依赖声明 | 声明式依赖注入，自动排序 |
| 生命周期 | 手动管理副作用 | fiber 自动回收副作用 |
| 适合 | 个人/轻量/快速 | 团队/企业/大型系统 |

**本质都是「把能力注册进一个宿主（host）」，区别在于「宿主的复杂度」。**

---

## 三、pi 的实现：Extensions + Skills + Packages

pi 的扩展是一个 **default export 的 TS 函数**，拿到 `ExtensionAPI`，能注册工具、命令、事件处理器、UI。

```ts
// ~/.pi/agent/extensions/deploy.ts
export default function (pi: ExtensionAPI) {
  pi.registerTool({ name: 'deploy', /* ... */ })
  pi.registerCommand('stats', { /* ... */ })
  pi.on('tool_call', async (event, ctx) => { /* 拦截 */ })
}
```

扩展能做的事（官方列举）：自定义工具 / 替换内置工具、子代理与 plan mode、自定义压缩与摘要、权限门与路径保护、自定义编辑器与 UI、SSH 与沙箱执行、MCP 集成……甚至能把它改得像 Claude Code。

配套机制：

- **Skills**：遵循 Agent Skills 标准的按需能力包（`SKILL.md`），`/skill:name` 或自动加载。
- **Prompt Templates**：可复用 Markdown 提示词，`/名字` 展开。
- **Pi Packages**：把扩展/skill/提示词/主题打包成 npm/git 包，`pi install npm:@foo/pi-tools` 分享。

**pi 的哲学**：「默认极简，按需扩展」。核心循环里什么都不内置，你要什么就写扩展加什么。

---

## 四、DSH 的实现：Everything is a Plugin + 自指工具集

DSH 用 Cordis 的依赖注入，把**一切能力都做成插件**：

```ts
// DSH 插件：命名导出 name/inject/apply
export const name = 'my-tool'
export const inject = ['tools']
export function apply(ctx) {
  ctx.tools.register({ name: 'lookup_customer', /* ... */ })
}
```

插件通过 `inject` 声明依赖、`provide` 提供服务，Cordis 负责加载顺序和**副作用自动回收**（fiber 生命周期）。

**DSH 最惊艳的设计是「自指工具集」**：模型可以在会话运行时，通过 `cordis_define` **现场定义并运行一个插件**：

| 工具 | 作用 |
|---|---|
| `cordis_inspect` | 只读查看服务、插件、工具、API 契约 |
| `cordis_define` | 记录一个动态包（host 半 + 可选 browser 半），只定义不运行 |
| `cordis_run` | 沙箱求值 host 半，投递 browser 半给 Web 页面 |
| `cordis_stop` | 卸载动态包，定义保留 |
| `cordis_undefine` | 停止并删除定义 |

这是「**元编程**」——harness 把「改造自己」的能力也交给了 agent。代价：动态包只存内存、重启即失、且不是安全边界。

---

## 五、我们如何设计：从轻量到重量

### 轻量版（学 pi）

```ts
interface ExtensionAPI {
  registerTool(tool: Tool): void
  registerCommand(name: string, handler: (args: string[]) => void): void
  on(event: string, handler: (payload: any) => void): () => void   // 返回 disposer
}

type Extension = (api: ExtensionAPI) => void | Promise<void>

class ExtensionHost {
  constructor(private registry: ToolRegistry, private emitter: Emitter) {}

  async load(ext: Extension) {
    const api: ExtensionAPI = {
      registerTool: t => this.registry.register(t),
      registerCommand: (n, h) => { /* 存命令表 */ },
      on: (ev, h) => { this.emitter.on(ev, h); return () => this.emitter.off(ev, h) },
    }
    await ext(api)
  }
}
```

### 重量版（学 DSH/Cordis）

```ts
class Context {
  private services = new Map<string, unknown>()
  private disposers: Array<() => void> = []

  provide<T>(name: string, value: T) { this.services.set(name, value) }
  get<T>(name: string): T { return this.services.get(name) as T }

  // 关键：effect 返回清理函数，卸载时自动执行
  effect(setup: () => void | (() => void)) {
    const dispose = setup()
    if (dispose) this.disposers.push(dispose)
  }

  async plugin(p: { inject?: string[]; apply(ctx: Context): void | Promise<void> }) {
    // 依赖没就绪 → 不加载（懒加载）
    for (const dep of p.inject ?? []) if (!this.services.has(dep)) return
    await p.apply(this)
  }

  async dispose() {
    for (const d of this.disposers) d()
    this.disposers = []
  }
}
```

**两者怎么选？** 个人工具、快速验证 → 轻量。要承载上千插件、要热更新、要多人协作 → 重量（DI + fiber 生命周期）。

---

## 六、设计原因与考虑维度

**为什么扩展机制是「灵魂」？** 因为它决定了一个 harness 能走多远：

- 没有扩展机制 → 每个需求都要改核心代码 → 无法形成生态。
- 有扩展机制 → 第三方能贡献能力 → 形成正循环（pi 的 Pi Packages、DSH 的插件生态）。

**设计扩展机制要考虑的维度**：

| 维度 | 要回答的问题 |
|---|---|
| 扩展粒度 | 只能加工具，还是能改核心行为？ |
| 生命周期 | 扩展的副作用如何加载、卸载、回收？ |
| 依赖管理 | 扩展之间如何声明和满足依赖？ |
| 隔离 | 扩展能否访问核心内部？越界怎么办？ |
| 分发 | 扩展如何打包、分享、版本管理？ |
| 安全 | 第三方扩展是任意代码，如何审查信任？ |

---

## 七、达成什么目标

读完本章，你应该能：

- ✅ 解释「轻量函数式」和「重量 DI 插件」两条路线的区别与取舍
- ✅ 说明 pi 的 Extensions/Skills/Packages 和 DSH 的自指工具集
- ✅ 分别写一个轻量 ExtensionHost 和一个重量 Context（DI + effect 回收）
- ✅ 说出扩展机制的 6 个考虑维度

---

## 八、实操练习

1. **写一个扩展**：用轻量 ExtensionHost 注册一个 `deploy` 工具，验证它能被核心循环调用。
2. **实现 effect 回收**：在重量 Context 里，让一个插件 `effect(() => setInterval(...))`，然后 `dispose()`，验证定时器被清理。
3. **思考题**：DSH 的动态包（`cordis_define`）「重启即失、不能自动晋升」，这个设计是缺陷还是特性？如果让你设计「动态包 → 正式插件」的晋升流程，你会怎么平衡安全与便捷？

---

> 下一章：[第 7 章 安全与隔离](07-安全与隔离.md) —— agent 会执行任意代码，怎么防止它闯祸。
